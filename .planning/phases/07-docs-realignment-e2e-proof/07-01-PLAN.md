---
phase: 07-docs-realignment-e2e-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/state-matrix-sync.test.ts
  - tests/e2e-v2-golden-path.test.ts
  - CLAUDE.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - TAX-02

must_haves:
  truths:
    - "Authoritative ADO State Matrix in ROADMAP.md strictly matches GOLDEN_PATH_V2 in code"
    - "A fixture ticket runs end-to-end through all 9 steps of Golden Path v2 on file-backed StateStore accumulating non-null L1-L7 evidence"
    - "Ticket is archived with complete L1-L7 evidence index after Done transition"
    - "Active project documentation and developer instructions describe the file-backed 5-column / 9-step / L1-L7 system without stale SQLite/Drizzle references"
  artifacts:
    - path: "tests/state-matrix-sync.test.ts"
      provides: "Automated drift guard for ROADMAP.md state matrix vs GOLDEN_PATH_V2"
      min_lines: 40
    - path: "tests/e2e-v2-golden-path.test.ts"
      provides: "End-to-end Golden Path v2 simulation test through all 9 steps"
      min_lines: 100
    - path: "CLAUDE.md"
      provides: "Realigned developer instructions with file-backed StateStore and no SQLite/Drizzle stack"
      contains: "StateStore"
    - path: ".planning/PROJECT.md"
      provides: "Realigned milestone goals and active status"
      contains: "Milestone **v2.0 — Golden Path v2**"
    - path: ".planning/REQUIREMENTS.md"
      provides: "All v2.0 requirements marked complete"
      contains: "[x] **TAX-02**"
  key_links:
    - from: "tests/state-matrix-sync.test.ts"
      to: "src/pipeline/taxonomy.ts"
      via: "GOLDEN_PATH_V2 import"
      pattern: "import.*GOLDEN_PATH_V2.*from.*taxonomy"
    - from: "tests/e2e-v2-golden-path.test.ts"
      to: "src/execute/router.ts"
      via: "routeWorkItemEvent"
      pattern: "routeWorkItemEvent\\(workItemId,"
    - from: "tests/e2e-v2-golden-path.test.ts"
      to: "src/deploy/evidence-index.ts"
      via: "compileL1L7EvidenceIndex"
      pattern: "compileL1L7EvidenceIndex\\(workItemId,.*failClosed: true"
---

<objective>
Prove the Golden Path v2 implementation end-to-end on the file-backed StateStore, guard against documentation-code drift between ROADMAP.md and GOLDEN_PATH_V2, and sweep out stale references to SQLite/Drizzle and v1 8-stage/L1-L6 models across active project docs.

Purpose: Complete Milestone v2.0 with verifiable proof that runtime routing and authoritative documentation strictly align with the 5-column / 9-step / L1–L7 architecture.
Output: `tests/state-matrix-sync.test.ts`, `tests/e2e-v2-golden-path.test.ts`, and updated `CLAUDE.md`, `PROJECT.md`, `REQUIREMENTS.md`, `ROADMAP.md`.
</objective>

<execution_context>
@$HOME/.config/antigravity/get-shit-done/workflows/execute-plan.md
@$HOME/.config/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-docs-realignment-e2e-proof/07-CONTEXT.md
@.planning/phases/07-docs-realignment-e2e-proof/07-RESEARCH.md
@.planning/phases/07-docs-realignment-e2e-proof/07-PATTERNS.md
@src/pipeline/taxonomy.ts
@src/execute/router.ts
@tests/lifecycle-replay.test.ts
@tests/deploy-orchestrator.test.ts

<interfaces>
From src/pipeline/taxonomy.ts:
```typescript
export type ColumnId = 'REFINEMENT' | 'EXECUTION' | 'ACCEPTANCE' | 'RELEASE' | 'RETRO';
export type StepNumber = 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | 9;
export type ActorRole = 'AI' | 'Human';
export type EvidenceLevel = 'L1' | 'L2' | 'L3' | 'L4' | 'L5' | 'L6' | 'L7';
export type AdoState = 'New' | 'Ready to Dev' | 'In Dev' | 'Dev Done' | 'Ready for QA' | 'Ready to Deploy' | 'Done';

export interface StepDefinition {
  readonly step: StepNumber;
  readonly name: string;
  readonly column: ColumnId;
  readonly actor: ActorRole;
  readonly actorDetail: string;
  readonly evidenceLevels: readonly EvidenceLevel[];
  readonly primaryEvidenceLevel: EvidenceLevel;
  readonly adoState: AdoState;
  readonly gateType: 'human_verdict' | 'automated_trigger';
  readonly keyTags?: readonly string[];
  readonly description?: string;
}

export const GOLDEN_PATH_V2: readonly StepDefinition[];
export function resolveRoutingStep(state: string, tags?: readonly string[] | string[] | string | null): StepDefinition | undefined;
```

From src/execute/router.ts:
```typescript
export async function routeWorkItemEvent(workItemId: number, revId: number, options?: any): Promise<void>;
```

From src/deploy/evidence-index.ts:
```typescript
export async function compileL1L7EvidenceIndex(workItemId: number, options?: { failClosed?: boolean }): Promise<L1L7EvidenceSummary>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated State Matrix Synchronization & Documentation Drift Guard (TAX-02)</name>
  <files>tests/state-matrix-sync.test.ts</files>
  <read_first>
    - src/pipeline/taxonomy.ts
    - .planning/ROADMAP.md
    - CLAUDE.md
    - .planning/PROJECT.md
  </read_first>
  <action>
    Create `tests/state-matrix-sync.test.ts` using Vitest to enforce synchronization between documentation and runtime taxonomy data without external markdown AST packages:
    1. Read `.planning/ROADMAP.md` using `node:fs` and locate section `## Authoritative ADO State Matrix (Golden Path v2 — 5 columns / 9 steps / L1–L7)`.
    2. Extract the markdown table rows for the 9 steps (starting with `| 1. REFINEMENT`, `| 2. EXECUTION`, `| 3. ACCEPTANCE`, `| 4. RELEASE`, `| 5. RETRO`).
    3. Assert exactly 9 step rows exist, matching `GOLDEN_PATH_V2.length`.
    4. For each row (0..8) compare with `GOLDEN_PATH_V2[index]`:
       - Column cell contains `stepDef.column`.
       - Step cell contains `stepDef.name`.
       - Actor cell contains `⚡` if `stepDef.actor === 'AI'` or `👤` if `stepDef.actor === 'Human'`.
       - ADO State cell matches `stepDef.adoState`.
       - Evidence cell contains all levels in `stepDef.evidenceLevels` (e.g. `L1`, `L2, L3`, `L7`).
       - Key Tags cell contains all tags in `stepDef.keyTags` if specified (e.g. `[awaiting-scope-lock]`, `[scope-locked]`).
    5. Add unit test asserting `CLAUDE.md` does not contain `better-sqlite3` or `drizzle-orm` (case-insensitive) and contains `StateStore`.
    6. Add unit test asserting `.planning/PROJECT.md` contains `5 columns, 9 actor-assigned steps, L1–L7 evidence`.
  </action>
  <acceptance_criteria>
    - `tests/state-matrix-sync.test.ts` exists and runs via Vitest.
    - Test validates all 9 steps against `GOLDEN_PATH_V2` with zero AST parser dependencies.
    - Fails if `ROADMAP.md` table differs in step name, actor emoji, state, evidence, or key tags.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/state-matrix-sync.test.ts</automated>
  </verify>
  <done>
    `tests/state-matrix-sync.test.ts` is implemented, exports comprehensive assertions for TAX-02, and passes via Vitest.
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-End 9-Step Golden Path v2 Simulation on File-Backed StateStore (TAX-02)</name>
  <files>tests/e2e-v2-golden-path.test.ts</files>
  <read_first>
    - src/pipeline/taxonomy.ts
    - src/execute/router.ts
    - src/deploy/worker.ts
    - tests/lifecycle-replay.test.ts
    - tests/deploy-orchestrator.test.ts
  </read_first>
  <action>
    Implement `tests/e2e-v2-golden-path.test.ts` simulating a fixture work item (#9901) advancing sequentially through all 9 steps on the file-backed `StateStore`:
    1. Initialize ephemeral test state store using `createTestStateStore()`, set `(env as any).STATE_STORE_DIR = harness.tempDir`, call `resetStateStore()`, and configure `afterEach` cleanup.
    2. Mock `adoClient.setWorkItemTrackingApi` with mock WIT API returning fixture revisions 1 through 9:
       - Rev 1 (`New`, tags: `backend`): Step 1 `Ticket & AC verify` runs via `routeWorkItemEvent(9901, 1)`. Verifies audit passes, persists `draft.auditLogs` (L1), sets `draft.scopeLock.status = 'pending'`, and parks ticket on `New` + `[awaiting-scope-lock]`.
       - Rev 2 (`Ready to Dev`, tags: `backend; [scope-locked]`, history: `[approve-scope]`): Step 2 `Scope review & verify` runs via `routeWorkItemEvent(9901, 2)`. Verifies `draft.scopeLock.status = 'locked'`.
       - Rev 3 (`In Dev`, tags: `backend; [scope-locked]`): Step 3 `Loop: Plan-Code-Test` runs via `routeWorkItemEvent(9901, 3)`. Call `recordL3Evidence` to persist valid L3 test evidence in `draft.l3Evidence` (8 tests passed, 0 failed, 350ms, diff stat: `2 files changed, 50 insertions(+)`).
       - Rev 4 (`Dev Done`, tags: `backend; [awaiting-acceptance]`): Step 4 `Dev validate & PR` runs via `routeWorkItemEvent(9901, 4)`. Verifies transition and tags.
       - Rev 5 (`Dev Done`, tags: `backend; [awaiting-acceptance]`, history: `Developer approved [approve-acceptance]`): Runs via `routeWorkItemEvent(9901, 5)`. Verifies tags become `[acceptance-approved]`.
       - Rev 6 (`Ready for QA`, tags: `backend; [acceptance-approved]; [pr-merged]`): Step 6 `QA staging verify` runs via `routeWorkItemEvent(9901, 6)`. Persist QA evidence in `draft.qaEvidence` (suite: `staging-integration`, passed: 12, failed: 0, 2-strike cleared).
       - Rev 7 & 8 (`Ready to Deploy`, tags: `backend; [qa-verified]; [deploying]`): Step 7 `Release approval + deploy` & Step 8 `Smoke test & monitor` and Step 9 `Retro` run via `processDeploymentWorkflow(9901, 8, deployOptions)` with `skipPreparation: true`, `mockSmokeResult: { outcome: 'passed' }`, `mockMetrics: { errorRatePercent: 0.01, p95LatencyMs: 120, totalRequests: 1500, failedRequests: 0, windowMinutes: 30 }`, `mockRetroResult: defaultMockRetro`, and `mockPrCreator`.
    3. Assert after Step 9 completion:
       - All 7 evidence tiers are non-null in StateStore: `auditLogs` (L1), `scopeLock` (L1), `l3Evidence` (L2/L3), `qaEvidence` (L3/L5), `deploymentRecords` (L5), `smokeEvidence` (L6), `telemetryEvaluations` (L6), `retroRecords` (L7), and `l7Evidence` (L7).
       - `compileL1L7EvidenceIndex(9901, { failClosed: true })` resolves successfully with all levels populated.
       - ADO update includes `System.State: Done` and tag `[golden-path-complete]`.
       - Archive file `path.join(harness.tempDir, 'archive', '9901.md')` exists and contains retro takeaways and action items.
  </action>
  <acceptance_criteria>
    - `tests/e2e-v2-golden-path.test.ts` drives ticket #9901 through all 9 steps.
    - All 7 evidence levels are non-null from persisted StateStore records.
    - Fail-closed compiler succeeds at Done state.
    - Ticket file is archived on disk in `archive/9901.md`.
    - Automated test passes cleanly.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/e2e-v2-golden-path.test.ts</automated>
  </verify>
  <done>
    `tests/e2e-v2-golden-path.test.ts` is implemented and verified green, providing complete end-to-end proof of Golden Path v2.
  </done>
</task>

<task type="auto">
  <name>Task 3: Sweep Documentation, Realign Stack in CLAUDE.md, and Finalize Milestone v2.0 (TAX-02)</name>
  <files>CLAUDE.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - CLAUDE.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
    Sweep active documentation to eliminate stale SQLite/Drizzle references, align with the 5-column / 9-step / L1-L7 file-backed architecture, and mark v2.0 requirements complete:
    1. In `CLAUDE.md`:
       - Under `### State Machine, Database & Queue`, replace `better-sqlite3` and `Drizzle ORM` entries with `**StateStore (node:fs)**` (version `built-in`, Purpose: `File-backed persistence`, Why: `Per-ticket markdown+frontmatter storage under data/state/tickets/<id>.md. Crash-atomic writes, zero C++ binaries.`, Confidence: `HIGH`).
       - Update `p-queue` entry to `p-queue / LaneManager` (Purpose: `Per-ticket serial lanes & throttle`, Why: `Serializes mutations per work item (concurrency: 1 single-writer) and limits active test runs.`).
       - Under `## Alternatives Considered`, replace `better-sqlite3 + Drizzle` alternative row with `File-backed StateStore` vs `better-sqlite3 / PostgreSQL`. Replace `SQLite table + p-queue` row with `LaneManager + p-queue` vs `BullMQ + Redis`.
    2. In `.planning/PROJECT.md`:
       - Under `### Active`, mark all 5 target feature checkboxes as checked:
         `- [x] **RESTRUCTURE**`
         `- [x] **L7 EVIDENCE**`
         `- [x] **PM SCOPE GATE**`
         `- [x] **PROD SMOKE**`
         `- [x] **RETRO OUTPUT**`
       - Update `## Key Decisions` table rows for Golden Path 7-column and L1-L6 entries to note supersedence by v2.0.
    3. In `.planning/REQUIREMENTS.md`:
       - Mark `TAX-02` as complete: `- [x] **TAX-02**: The ADO state-transition router and the evidence-index stage labels are driven by the v2 taxonomy, and the authoritative state matrix in ROADMAP/docs reflects the 5 columns / 9 steps / L1–L7 model with governance hand-offs.`
       - In `## Traceability`, update `TAX-02` status to `Complete`.
    4. In `.planning/ROADMAP.md`:
       - Update Phase 7 progress table entry to `1/1` complete with date `2026-09-18`.
       - Update Phase 7 status from `Not started` to `Complete`.
       - Update Plans list to mark `07-01-PLAN.md` checked.
    5. Run `npx vitest run tests/state-matrix-sync.test.ts` to confirm doc assertions pass.
    6. Run `npm test` across all suites to confirm full repository green.
  </action>
  <acceptance_criteria>
    - `CLAUDE.md` has zero occurrences of `better-sqlite3` and `drizzle-orm` in active stack descriptions.
    - `.planning/PROJECT.md` reflects all 5 target features completed.
    - `.planning/REQUIREMENTS.md` marks TAX-02 complete with 19/19 v2.0 requirements complete.
    - `.planning/ROADMAP.md` reflects Phase 7 complete.
    - `npx vitest run tests/state-matrix-sync.test.ts` and `npm test` pass.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/state-matrix-sync.test.ts tests/e2e-v2-golden-path.test.ts</automated>
  </verify>
  <done>
    Documentation is realigned with the built file-backed Golden Path v2 system, all requirements are marked complete, and all tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → Coding Agents | System instructions, stack definitions, and planning artifacts consumed by future LLM agents |
| StateStore → File System | Ephemeral test state directories isolated from persistent project runtime data |
| ADO Wire State → Router | State and tag strings mapped to immutable taxonomy step definitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Repudiation | `tests/state-matrix-sync.test.ts` | mitigate | Automated markdown table AST check parses Authoritative ADO State Matrix from `ROADMAP.md` and asserts exact parity with `GOLDEN_PATH_V2`; CI fails on drift |
| T-07-02 | Tampering | `tests/e2e-v2-golden-path.test.ts` | mitigate | Use ephemeral `createTestStateStore()` with `cleanup()` in `afterEach` to isolate E2E state mutations from workspace files |
| T-07-03 | Information Disclosure | `CLAUDE.md`, `.planning/PROJECT.md` | mitigate | Sweep out stale references to `better-sqlite3`, `drizzle-orm`, and `src/db/` to prevent agents from attempting database connections or leaking credential patterns |
</threat_model>

<verification>
Automated verification commands:
1. `npx vitest run tests/state-matrix-sync.test.ts` verifies state matrix alignment and clean docs.
2. `npx vitest run tests/e2e-v2-golden-path.test.ts` verifies 9-step simulation on StateStore.
3. `npm test` verifies full repository suite (all 45+ test files green).
</verification>

<success_criteria>
- State matrix sync test asserts zero drift between `ROADMAP.md` and `src/pipeline/taxonomy.ts`.
- Fixture ticket #9901 walks through all 9 steps of Golden Path v2 with non-null L1-L7 evidence and successful ticket archiving.
- Active documentation (`CLAUDE.md`, `PROJECT.md`, `REQUIREMENTS.md`, `ROADMAP.md`) provably describes the built file-backed Golden Path v2 system with zero stale SQLite/Drizzle references.
- 100% of Milestone v2.0 requirements (19/19) are verified complete.
</success_criteria>

<output>
After completion, create `.planning/phases/07-docs-realignment-e2e-proof/07-01-SUMMARY.md`
</output>
