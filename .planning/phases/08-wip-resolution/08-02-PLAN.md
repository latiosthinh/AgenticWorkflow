---
phase: 08-wip-resolution
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/state/types.ts
  - src/plan/checkpoint.ts
  - src/auditor/worker.ts
  - src/execute/worker.ts
  - src/plan/formatter.ts
  - tests/plan-checkpoint.test.ts
  - tests/worker.test.ts
  - tests/planner.test.ts
  - tests/wip-static-guards.test.ts
  - src/cli/ado.ts
autonomous: true
requirements: [WIP-02, WIP-04, WIP-05]

must_haves:
  truths:
    - "Persisted audit records carry fallbackUsed + the actual/resolved model — no audit-log entry hardcodes model:'gpt-4o'"
    - "Planner fallback usage (fallbackUsed + model) persists on the PlanCheckpointState (L2 evidence) through the lane-disciplined createPlanCheckpoint path"
    - "The opencode plan-skip path persists planDelegated:true + planNote on the locked plan checkpoint and surfaces the note in the ADO plan-locked comment as an auditable governance deviation"
    - "src/cli/ado.ts is committed as-is (tracked, zero content changes); git status is clean at phase end"
    - "Full suite green: existing 462 tests + all new WIP tests, no §Done-well regression"
  artifacts:
    - path: "src/state/types.ts"
      provides: "additive optional fields — AuditLogEntry.fallbackUsed?; PlanCheckpointState.fallbackUsed?/model?/planDelegated?/planNote? (l7Evidence additive pattern, backwards-compatible with existing state files)"
      contains: "planDelegated"
    - path: "src/plan/checkpoint.ts"
      provides: "CreateCheckpointInput passthrough for fallbackUsed/model/planDelegated/planNote into the persisted checkpoint entry (existing lane-context/runInLane discipline unchanged)"
      contains: "planNote"
    - path: "src/auditor/worker.ts"
      provides: "both audit-log pushes persist model: result.model + fallbackUsed: result.fallbackUsed (hardcoded 'gpt-4o' at :84,:116 removed)"
    - path: "src/execute/worker.ts"
      provides: "skip branch stamps planDelegated:true + planNote; both createPlanCheckpoint calls pass fallbackUsed/model (+planDelegated/planNote on locked branch); plan-locked comment carries the governance note"
      contains: "planDelegated: true"
    - path: "src/plan/formatter.ts"
      provides: "formatPlanLockedComment optional 3rd param governanceNote rendered as a sanitized '**Governance Note:**' section; loop shield unchanged"
      contains: "Governance Note"
    - path: "tests/wip-static-guards.test.ts"
      provides: "permanent guards — no 'gpt-4o' literal in src/auditor/worker.ts; skip branch in src/execute/worker.ts keeps planDelegated/planNote/forceAiPlanner escape"
    - path: "src/cli/ado.ts"
      provides: "tracked as-is; wire-or-delete + v1 vocabulary fix recorded for Phase 12 QAL-03"
  key_links:
    - from: "src/auditor/evaluator.ts AuditOutcome (plan 08-01)"
      to: "src/auditor/worker.ts auditLogs.push"
      via: "result.model + result.fallbackUsed persisted through stateStore.updateTicketState"
      pattern: "model: result\\.model"
    - from: "src/execute/worker.ts skip branch"
      to: "TicketState.planCheckpoints (L2 evidence)"
      via: "createPlanCheckpoint → stateStore.updateTicketState inside lane discipline (checkpoint.ts:36-40)"
      pattern: "planDelegated"
    - from: "src/execute/worker.ts locked-plan comment"
      to: "ADO work item System.History"
      via: "formatPlanLockedComment(planMarkdown, estimatedFiles, planNote) → sanitizeHtml + loop shield → adoClient.updateWorkItem"
      pattern: "Governance Note"
---

<objective>
Persist the fallback evidence and commit the phase: wire AuditOutcome/PlannerOutcome fields (from plan 08-01) into persisted L1 audit records and L2 plan checkpoints (WIP-02), record + surface the opencode plan-skip governance deviation (WIP-04), commit src/cli/ado.ts as-is per the recorded decision, and end with a clean tree + full green suite (WIP-05).

Purpose: Fallbacks that aren't persisted are repudiable; a plan-skip that isn't recorded is an invisible governance deviation. This plan makes both auditable on the work item and closes the commit-forward gate every later phase depends on.
Output: 3 atomic commits (evidence persistence, plan-skip record, cli-as-is) + verified clean `git status` + full suite green.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wip-resolution/08-CONTEXT.md
@.planning/phases/08-wip-resolution/08-01-SUMMARY.md
@src/state/types.ts
@src/plan/checkpoint.ts
@src/auditor/worker.ts
@src/plan/formatter.ts

<interfaces>
<!-- Produced by plan 08-01 (already committed when this plan runs). Use directly — no exploration needed. -->

From src/auditor/evaluator.ts (08-01 Task 3):
```typescript
export type AuditOutcome = AuditResult & { fallbackUsed: boolean; model: string };
export async function auditTicketContract(...): Promise<AuditOutcome>;
// deterministic test-env path: fallbackUsed:false, model: env.API_MODEL ('gpt-4o' under vitest)
```

From src/plan/planner.ts (08-01 Task 2):
```typescript
export type PlannerOutcome = PlanResult & { fallbackUsed: boolean; model: string };
export async function formulateImplementationPlan(ticket, opts?): Promise<PlannerOutcome>;
// LLM-failure catch: { hasAmbiguities: true, questions: ['LLM planning unavailable — human plan review required'], ..., fallbackUsed: true, model: env.API_MODEL }
```

From src/plan/checkpoint.ts (extended in this plan, Task 1):
```typescript
export interface CreateCheckpointInput {
  workItemId: number; revId: number; questions: string[];
  planMarkdown?: string; estimatedFiles?: string[]; testStrategy?: string;
  fallbackUsed?: boolean; model?: string; planDelegated?: boolean; planNote?: string; // NEW additive
}
```

From src/execute/worker.ts (uncommitted WIP being completed — Task 2 territory):
```typescript
// :404-420 skip branch (env.LOCAL_AGENT_TYPE === 'opencode' && env.NODE_ENV !== 'test' && !options?.forceAiPlanner)
// :422-444 if (plan.hasAmbiguities) → createPlanCheckpoint + [Plan Q&A] comment + worktree release (UNCHANGED routing)
// :445-478 else → createPlanCheckpoint + updateCheckpointStatus(cp.id,'locked') + formatPlanLockedComment + System.History patch
```

From src/state/store.ts (read-only fact): TicketState frontmatter is serialized whole via JSON.stringify — additive optional fields persist with ZERO store changes; existing state files parse unchanged (fields optional).
</interfaces>

<constraints>
- No new dependencies. TypeScript 7 + Vitest 5 + ESM.
- Additive-only type extensions (pattern: l7Evidence) — no required fields, no renames, backwards-compatible with existing data/state/tickets/*.md files.
- All TicketState writes go through existing lane-disciplined paths (createPlanCheckpoint's runInLane/lane-context check; auditor worker's stateStore.updateTicketState inside its existing call site). Do NOT add direct fs writes or bypass lanes.
- Do NOT fix src/cli/ado.ts issues (v1 vocabulary 'To Do'/'Doing', raw WIQL interpolation) — recorded decision: commit as-is, wire-or-delete in Phase 12 QAL-03. Zero content changes to the file.
- Do NOT touch the skip condition itself (keep `LOCAL_AGENT_TYPE=opencode` + non-test + `!forceAiPlanner` escape hatch) and do NOT touch the uncommitted src/auditor/prompt.ts JSON-schema text — it rides along in the Task 3 catch-all commit as-is.
- Do NOT pre-empt Phase 9 (no prompt XML isolation, no built-in path deletion) or Phase 10+ (no fallback recording for retro/learn call sites).
- §Done-well invariants protected: formatPlanLockedComment's sanitizeHtml + `<!-- [automated-agent] -->` loop shield stay exactly as-is; the governanceNote param feeds the markdown BEFORE sanitization.
- tests/worker.test.ts:80 asserts auditLogs[0].model === 'gpt-4o' — stays green after the rewire because the deterministic test-env path resolves model to env.API_MODEL, which defaults to 'gpt-4o' under vitest. Do NOT weaken that assertion; add fallbackUsed assertion next to it.
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist fallbackUsed + actual model into L1 audit records and L2 plan checkpoints (WIP-02)</name>
  <files>src/state/types.ts, src/plan/checkpoint.ts, src/auditor/worker.ts, tests/plan-checkpoint.test.ts, tests/worker.test.ts, tests/wip-static-guards.test.ts (new)</files>
  <behavior>
    - createPlanCheckpoint({ ..., fallbackUsed: true, model: 'm-x', planDelegated: true, planNote: 'n' }) → getTicketState roundtrip shows all four fields on planCheckpoints[0]; omitting them → fields absent/undefined after roundtrip (backwards compatible)
    - processWorkItemAudit (existing worker.test.ts harness, deterministic test-env evaluator) → auditLogs[0].model === 'gpt-4o' (env-resolved, existing assertion untouched) AND auditLogs[0].fallbackUsed === false (new assertion, both passed and failed verdict paths)
    - Static guard: src/auditor/worker.ts source contains no 'gpt-4o' string literal
  </behavior>
  <action>
    1. src/state/types.ts — additive optional fields only:
       - AuditLogEntry: add `fallbackUsed?: boolean;`
       - PlanCheckpointState: add `fallbackUsed?: boolean;`, `model?: string | null;`, `planDelegated?: boolean;`, `planNote?: string | null;`
    2. src/plan/checkpoint.ts — extend CreateCheckpointInput with `fallbackUsed?: boolean; model?: string; planDelegated?: boolean; planNote?: string;` and in the mutator's `created` object add `fallbackUsed: data.fallbackUsed, model: data.model ?? null, planDelegated: data.planDelegated, planNote: data.planNote ?? null,` (JSON.stringify drops undefined — old checkpoints and callers unaffected). Do NOT touch the lane-context/runInLane logic (:36-40) — it already provides the lane discipline this persistence rides on.
    3. src/auditor/worker.ts — in BOTH auditLogs.push blocks (:79-86 passed, :111-118 failed) replace `model: 'gpt-4o',` with `model: result.model,` and add `fallbackUsed: result.fallbackUsed,`. Nothing else changes.
    4. tests/plan-checkpoint.test.ts — extend the CRUD test (or add one `it`) to pass the four new fields to createPlanCheckpoint and assert them after `stateStore.getTicketState`; add a second checkpoint created WITHOUT the new fields asserting `planDelegated` is undefined (backwards compat).
    5. tests/worker.test.ts — next to the existing `:80` model assertion add `expect(ticket?.auditLogs[0].fallbackUsed).toBe(false);` and add the same assertion to the failed-verdict test (~:131-135).
    6. Create tests/wip-static-guards.test.ts: readFileSync src/auditor/worker.ts → `expect(src).not.toMatch(/gpt-4o/)`; (Task 2 appends more guards to this file.)
  </action>
  <verify>
    <automated>npx vitest run tests/plan-checkpoint.test.ts tests/worker.test.ts tests/wip-static-guards.test.ts tests/auditor.test.ts; if ($?) { npx tsc --noEmit }</automated>
  </verify>
  <done>Audit records persist env-resolved/actual model + fallbackUsed; checkpoint passthrough live; zero 'gpt-4o' literals in src/auditor/; targeted tests + typecheck green. Commit: `feat(08-02): persist fallbackUsed + actual model in L1/L2 evidence (WIP-02)` staging ONLY this task's 6 files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record + surface the opencode plan-skip governance deviation (WIP-04)</name>
  <files>src/execute/worker.ts, src/plan/formatter.ts, tests/planner.test.ts, tests/wip-static-guards.test.ts</files>
  <behavior>
    - formatPlanLockedComment(md, files, 'plan delegated to opencode') → sanitized HTML containing 'Governance Note' + the note text + `<!-- [automated-agent] -->` shield; 2-arg calls render identically to before (existing formatter tests untouched)
    - formatPlanLockedComment sanitizes a hostile note: '<img src=x onerror=alert(1)>' → no 'onerror' in output
    - Skip-branch plan object carries planDelegated:true + planNote:'plan delegated to opencode' and both createPlanCheckpoint calls receive the evidence fields (static guards — the branch is unreachable under vitest's NODE_ENV=test by design; integration coverage is Phase 13 E2E territory)
    - Existing execute-pipeline tests (ambiguity flow :280, resumption flow :335, router dispatches) stay green
  </behavior>
  <action>
    1. src/plan/formatter.ts — add optional 3rd param: `export function formatPlanLockedComment(planMarkdown: string, estimatedFiles: string[], governanceNote?: string): string`. When truthy, insert into the markdown template directly after the `### [Plan Checkpoint] Implementation Plan Locked` header line:
       ```
       **Governance Note:** ${governanceNote}
       ```
       (blank line after). Everything downstream — marked.parse, sanitizeHtml allowlist, loop-shield suffix — unchanged; the note rides THROUGH sanitization. Keep the trailing `// ponytail:` comment.
    2. src/execute/worker.ts — three edits, nothing else:
       a. Skip branch (:405-412): add `planDelegated: true,` and `planNote: 'plan delegated to opencode',` to the plan object literal (condition + existing fields untouched).
       b. Ambiguity-branch createPlanCheckpoint (:423-430): add `fallbackUsed: plan.fallbackUsed, model: plan.model,` to the input object.
       c. Locked-branch createPlanCheckpoint (:446-453): add `fallbackUsed: plan.fallbackUsed, model: plan.model, planDelegated: plan.planDelegated, planNote: plan.planNote,`; and change the comment call (:456-459) to `formatPlanLockedComment(plan.planMarkdown, plan.estimatedFiles, plan.planNote)`.
       `plan` is already `any` — leave it (Phase 9 CORE-01 rewires this block; do not pre-empt).
    3. tests/planner.test.ts — in the 'Plan Comment Formatters' describe add two `it`s: governanceNote rendering (+ shield present, 2-arg unchanged) and hostile-note sanitization per the behavior block.
    4. tests/wip-static-guards.test.ts — append guards reading src/execute/worker.ts: contains `planDelegated: true`, contains `'plan delegated to opencode'`, skip condition still contains `forceAiPlanner`, and locked-branch comment call matches /formatPlanLockedComment\([^)]*plan\.planNote/ (dotAll flag or [\s\S] class — the call spans lines).
  </action>
  <verify>
    <automated>npx vitest run tests/planner.test.ts tests/wip-static-guards.test.ts tests/plan-checkpoint.test.ts; if ($?) { npx tsc --noEmit }</automated>
  </verify>
  <done>Skip path persists planDelegated/planNote via lane-disciplined createPlanCheckpoint and surfaces the note in the sanitized ADO plan-locked comment; guards permanent; targeted tests + typecheck green. Commit: `feat(08-02): record plan-delegated-to-opencode governance deviation in L2 evidence (WIP-04)` staging ONLY this task's 4 files.</done>
</task>

<task type="auto">
  <name>Task 3: Commit remaining WIP as-is + full-suite verification + clean tree (WIP-05)</name>
  <files>src/cli/ado.ts (add as-is), src/auditor/prompt.ts (commit uncommitted diff as-is)</files>
  <action>
    1. Verify zero unintended drift first: `git status --porcelain` must show exactly ` M src/auditor/prompt.ts` + `?? src/cli/` and nothing else (provider/planner/evaluator WIP committed by plan 08-01; execute/worker.ts WIP committed by Task 2 above). If anything else is dirty, STOP and investigate — do not commit unknown changes.
    2. Commit src/auditor/prompt.ts as-is (uncommitted WIP: JSON-schema response instruction — keep verbatim, no edits): `git add src/auditor/prompt.ts; git commit -m "feat(08-02): auditor prompt requests pure-JSON schema response (WIP as-is)"`.
    3. Commit src/cli/ado.ts AS-IS — untracked → tracked, zero content changes (recorded decision 08-CONTEXT: wire-or-delete + v1 vocabulary fix executes in Phase 12 QAL-03; v1 column names and WIQL string interpolation are KNOWN issues, deliberately NOT fixed here):
       `git add src/cli/ado.ts; git commit -m "chore(08-02): track src/cli/ado.ts as-is - wire-or-delete deferred to Phase 12 QAL-03 (recorded decision)"`.
    4. Full verification gate (all must pass):
       - `npm test` → 48+ files, 462 existing + all new WIP tests, 0 failures
       - `npx tsc --noEmit` → clean
       - `rg "model: 'gpt-4o'" src` → zero matches (opencode-runner's `|| 'gpt-4o'` env-default chains are NOT audit-log hardcodes and stay — Phase 12 CFG territory)
       - `rg "catch\s*\{\s*\}" src/ai/provider.ts` → zero matches
       - `git status --porcelain` → EMPTY (clean tree — phase gate)
       - `git log --oneline -8` → the phase's atomic commits present (3 from 08-01, 3 from 08-02)
    5. If git reports CRLF/LF warnings only — that is cosmetic (core.autocrlf), not dirt; `git status --porcelain` empty is the gate.
  </action>
  <verify>
    <automated>npm test 2>&1 | Select-Object -Last 5; if ($?) { npx tsc --noEmit }; if ($?) { git status --porcelain }</automated>
  </verify>
  <done>Working tree clean (`git status --porcelain` empty), src/cli/ado.ts tracked with zero content changes, full suite green (462 + new), typecheck clean, no hardcoded audit-log model, no silent catch. Commit: only the two commits above — no extra files staged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator → StateStore (evidence persistence) | governance-critical records; must be complete + lane-serialized |
| orchestrator → ADO work item (comments) | rendered to humans; must stay sanitized + loop-shielded |
| developer machine → repo (cli commit) | untracked script with known v1 issues enters version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Repudiation | fallback/plan-skip not visible in persisted evidence or on the work item | mitigate | fallbackUsed+model on AuditLogEntry/PlanCheckpointState; planDelegated+planNote persisted AND surfaced in the ADO plan-locked comment (Tasks 1-2); static guards make removal test-failing |
| T-08-06 | Tampering | governanceNote text injected into ADO comment HTML | mitigate | note is rendered into markdown BEFORE marked.parse + sanitizeHtml allowlist + loop shield (existing §Done-well formatter pipeline); hostile-note sanitization test in Task 2 |
| T-08-07 | Tampering | concurrent evidence writes racing per ticket | mitigate | no new write paths — persistence rides existing lane-disciplined createPlanCheckpoint (runInLane/lane-context, checkpoint.ts:36-40) and the auditor worker's existing updateTicketState call site; lane single-writer invariant untouched |
| T-08-08 | Information Disclosure / Tampering | src/cli/ado.ts committed as-is (raw `${env.ADO_PROJECT}` WIQL interpolation, v1 state vocabulary) | accept | file is not wired into any runtime path (no bin entry, no importer — dead script); recorded decision defers wire-or-delete + vocabulary fix to Phase 12 QAL-03; committing restores tree hygiene which gates ALL later phases |
| T-08-09 | Denial of Service | malformed planNote/oversized fields in state files | accept | values are orchestrator-internal constants ('plan delegated to opencode'), not user input; JSON frontmatter serialization unchanged |
</threat_model>

<verification>
1. `npm test` → 48+ files green, test count ≥ 462 + new tests from both plans (~20+).
2. `npx tsc --noEmit` → clean.
3. `git status --porcelain` → empty (SC5 clean-tree gate).
4. `rg "gpt-4o" src/auditor` → zero matches; `rg "planDelegated" src/execute/worker.ts src/state/types.ts src/plan/checkpoint.ts` → matches in all three.
5. §Done-well spot check: formatter.ts still ends with sanitizeHtml + `<!-- [automated-agent] -->` suffix; lane-manager, store.ts, dedup untouched (`git diff HEAD~6 --stat` shows no edits to them).
6. ROADMAP SC mapping: SC2 ← Task 1 (+08-01), SC4 ← Task 2, SC5 ← Task 3.
</verification>

<success_criteria>
- SC2 (ROADMAP Phase 8 #2) fully satisfied: fallback firing records fallbackUsed:true + actual model in persisted evidence; no audit-log entry hardcodes model:'gpt-4o' (static-guarded).
- SC4 satisfied: opencode plan-skip writes planDelegated/planNote into L2 plan-checkpoint evidence AND surfaces it on the work item via the sanitized plan-locked comment.
- SC5 satisfied: git status clean; src/cli/ resolved by the recorded as-is-commit decision for QAL-03; full suite (462 + new) green; no §Done-well regression.
- Additive-only type changes — existing data/state/tickets/*.md files still parse (all new fields optional).
</success_criteria>

<source_audit>
| Item | Source | Coverage |
|------|--------|----------|
| GOAL: dirty tree committed under phase discipline, clean tree, governance-safe fallbacks | ROADMAP Phase 8 | Tasks 1-3 |
| WIP-02 (persistence + hardcode removal) | REQUIREMENTS | Task 1 (producer half → 08-01 Tasks 2-3) |
| WIP-04 | REQUIREMENTS | Task 2 |
| WIP-05 | REQUIREMENTS | Task 3 |
| L9 (plan-skip governance regression → record delegation; zero tests) | AUDIT-v2.1 | Task 2 |
| M7 (audit log hardcodes model:'gpt-4o' at worker.ts:84,116) | AUDIT-v2.1 | Task 1 |
| CONTEXT: cli as-is commit (QAL-03 Phase 12); additive l7Evidence-pattern types; lane-disciplined writes; note surfaced on work item | 08-CONTEXT decisions | Tasks 1-3 + constraints |
| Deferred (NOT here): cli fixes → Phase 12; prompt isolation → Phase 9; retro/learn fallback recording → Phase 10+ | 08-CONTEXT deferred | Excluded by design |
</source_audit>

<output>
After completion, create `.planning/phases/08-wip-resolution/08-02-SUMMARY.md`
</output>
