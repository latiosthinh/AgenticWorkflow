---
phase: 08-wip-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/provider.ts
  - tests/provider.test.ts
  - src/plan/planner.ts
  - tests/planner-fallback.test.ts
  - src/auditor/evaluator.ts
  - tests/auditor-fallback.test.ts
autonomous: true
requirements: [WIP-01, WIP-02, WIP-03]

must_haves:
  truths:
    - "9router request bodies are rewritten (stream:false injected, 9router/ model prefix stripped) by an exported pure helper, and rewrite failures are logged via console.warn and forward the original body — no silent catch {}"
    - "A planner LLM failure returns hasAmbiguities:true with question 'LLM planning unavailable — human plan review required' (no boilerplate auto-proceed plan) plus fallbackUsed:true and the resolved model"
    - "Every auditTicketContract return path carries fallbackUsed + model (actual response model when available, else resolved env.API_MODEL); the LLM-failure path sets fallbackUsed:true"
    - "NODE_ENV=test deterministic branches in evaluator and planner keep working unchanged (existing 462-test suite green)"
  artifacts:
    - path: "src/ai/provider.ts"
      provides: "exported rewriteRouterBody(body) pure helper; customStreamFetch consumes it; console.warn on failure"
      contains: "export function rewriteRouterBody"
    - path: "src/plan/planner.ts"
      provides: "PlannerOutcome = PlanResult & { fallbackUsed: boolean; model: string }; catch branch parks for human"
      contains: "LLM planning unavailable"
    - path: "src/auditor/evaluator.ts"
      provides: "AuditOutcome = AuditResult & { fallbackUsed: boolean; model: string } on all return paths"
      contains: "fallbackUsed"
    - path: "tests/provider.test.ts"
      provides: "rewrite success + failure-branch + getModel prefix-strip tests; static no-catch{} guard"
    - path: "tests/planner-fallback.test.ts"
      provides: "planner LLM-failure park test + success model-capture test + caller-routing static guard (vi.mock 'ai')"
    - path: "tests/auditor-fallback.test.ts"
      provides: "evaluator fallback evidence tests (vi.mock 'ai')"
  key_links:
    - from: "src/ai/provider.ts customStreamFetch"
      to: "rewriteRouterBody"
      via: "const body = rewriteRouterBody(init?.body) passed to fetch"
      pattern: "rewriteRouterBody\\(init\\?\\.body\\)"
    - from: "src/plan/planner.ts catch branch"
      to: "Plan-Q&A checkpoint routing in src/execute/worker.ts:422 (existing, unchanged here)"
      via: "hasAmbiguities:true shape that worker's if (plan.hasAmbiguities) branch consumes"
      pattern: "hasAmbiguities: true"
    - from: "src/auditor/evaluator.ts"
      to: "src/auditor/worker.ts (persistence wired in plan 08-02)"
      via: "AuditOutcome fields returned to caller"
      pattern: "fallbackUsed"
---

<objective>
Complete the uncommitted WIP fallback mechanics with tests: extract the 9router body-rewrite into an exported pure helper with explicit failure logging (WIP-01), make the planner LLM-failure fallback park the ticket for human review instead of auto-proceeding a boilerplate plan (WIP-03), and make evaluator + planner returns carry `fallbackUsed` + actual model so plan 08-02 can persist them (WIP-02, producer half).

Purpose: The dirty working tree currently contains gate-bypassing fallbacks and a silent `catch {}` — this plan makes them tested, honest, and governance-safe WITHOUT reverting any WIP behavior that stays (stream:false injection, prefix strip, deterministic test branches, console.warn logging).
Output: 3 atomic commits on top of the dirty tree (provider, planner, evaluator), ~15 new tests, full suite green.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wip-resolution/08-CONTEXT.md
@src/ai/provider.ts
@src/plan/planner.ts
@src/auditor/evaluator.ts
@tests/provider.test.ts

<interfaces>
<!-- Contracts this plan produces; plan 08-02 consumes AuditOutcome/PlannerOutcome. -->

From src/plan/schema.ts (unchanged — base shapes):
```typescript
export const PlanResultSchema = z.object({
  hasAmbiguities: z.boolean(), questions: z.array(z.string()),
  planMarkdown: z.string(), estimatedFiles: z.array(z.string()), testStrategy: z.string(),
});
export type PlanResult = z.infer<typeof PlanResultSchema>;
```

From src/auditor/schema.ts (unchanged — base shape):
```typescript
export type AuditResult = { passed: boolean; reasons: string[]; criteria_summary: string };
```

From src/config/env.ts (read-only):
```typescript
export const env: { NODE_ENV: 'development'|'test'|'production'; API_MODEL: string; /* default 'gpt-4o' */ ... };
```

Existing caller (do NOT modify in this plan — plan 08-02 wires persistence):
- src/execute/worker.ts:404-430 — `let plan: any; ... if (plan.hasAmbiguities) { await createPlanCheckpoint(...) }` — the fallback shape from this plan flows into that EXISTING branch untouched.
- src/execute/rework-worker.ts — verified 2026-09-19: contains ZERO references to formulateImplementationPlan/hasAmbiguities/createPlanCheckpoint (rework reuses the locked plan). Task 2 adds a permanent static guard for this.
</interfaces>

<constraints>
- No new dependencies. TypeScript 7 + Vitest 5 + ESM. `vi.mock` precedent exists (4/48 test files).
- Do NOT touch §Done-well invariants (AUDIT-v2.1.md): HMAC, wx-dedup, lane single-writer, sanitizing formatters, crash-atomic writes, fail-closed telemetry, circuit breakers, withRetry.
- Do NOT pre-empt Phase 9: no prompt XML isolation changes, no built-in-path deletion, no escapeXml work. The uncommitted prompt.ts JSON-schema instruction text stays as-is.
- Keep NODE_ENV=test deterministic branches in evaluator (evaluator.ts:148) and planner (planner.ts:22) working — many of the 462 tests depend on them.
- Keep the WIP behavior exactly: `stream:false` default injection + `9router/` prefix strip in body rewrite AND getModel(). Keep console.warn fallback logging style.
- Vitest config pins NODE_ENV=test + OPENAI_API_KEY=test-openai-api-key; env is parsed once at import — LLM paths are only reachable in tests via `forceAi: true` + vi.mock('ai').
- Only field-level assertions exist on these results (no whole-object toEqual in tests/auditor.test.ts, tests/planner.test.ts) — additive return fields are safe.
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract rewriteRouterBody pure helper + failure logging (WIP-01)</name>
  <files>src/ai/provider.ts, tests/provider.test.ts</files>
  <behavior>
    - rewriteRouterBody('{"model":"9router/deepseek-chat"}') → JSON string with model "deepseek-chat" and stream:false injected
    - rewriteRouterBody('{"stream":true,"model":"m"}') → stream:true preserved, model untouched
    - rewriteRouterBody(undefined) / non-string body → returned as-is
    - rewriteRouterBody('not json{') → original string returned AND console.warn called once (no silent catch {})
    - rewriteRouterBody('"scalar"') / '123' (valid JSON primitives) → returned as-is, no warn
    - getModel('9router/x') → modelId 'x'
    - customStreamFetch end-to-end (stubbed global fetch): body arrives rewritten, authorization header set from env.API_KEY when present
    - Static guard: src/ai/provider.ts source contains no empty-catch pattern
  </behavior>
  <action>
    In src/ai/provider.ts: add exported pure helper (no env, no fetch, no side effects except the warn):

    ```typescript
    export function rewriteRouterBody(body: unknown): unknown {
      if (!body || typeof body !== 'string') return body;
      try {
        const parsed = JSON.parse(body);
        if (!parsed || typeof parsed !== 'object') return body; // JSON primitives: forward untouched
        if (parsed.stream === undefined) parsed.stream = false;
        if (typeof parsed.model === 'string' && parsed.model.startsWith('9router/')) {
          parsed.model = parsed.model.replace('9router/', '');
        }
        return JSON.stringify(parsed);
      } catch (err) {
        console.warn('[provider] request body rewrite failed; forwarding original body:', (err as any)?.message || err);
        return body;
      }
    }
    ```

    Replace the inline try/catch block in customStreamFetch (lines 13-25) with `const body = rewriteRouterBody(init?.body);` — the silent `catch {}` is deleted by this replacement. Behavior for object JSON bodies is identical to the WIP; the object-guard makes primitive JSON bodies forward cleanly instead of throwing-into-catch (edge-case-correct, same observable outcome). Keep the `// ponytail:` comment at file end untouched.

    In tests/provider.test.ts: add `rewriteRouterBody` to the existing import from '../src/ai/provider.js'; extend the describe with the behavior cases above. For the failure branch use `vi.spyOn(console, 'warn').mockImplementation(() => {})` and restore (`mockRestore`) after. For the customStreamFetch integration case use `vi.stubGlobal('fetch', vi.fn(async () => new Response('{}')))` , call `customStreamFetch('http://localhost:9999/v1/chat', { body: JSON.stringify({ model: '9router/m', messages: [] }) })`, assert the stub received init.body parsing to `{ stream: false, model: 'm', messages: [] }`, then `vi.unstubAllGlobals()`. For the static guard read the source with `node:fs` (`readFileSync(new URL('../src/ai/provider.ts', import.meta.url), 'utf8')`) and `expect(src).not.toMatch(/catch\s*\{\s*\}/)`. For getModel strip: `expect(getModel('9router/deepseek-chat').modelId).toBe('deepseek-chat')`.
  </action>
  <verify>
    <automated>npx vitest run tests/provider.test.ts; if ($?) { npx tsc --noEmit }</automated>
  </verify>
  <done>rewriteRouterBody exported and consumed by customStreamFetch; zero `catch {}` in src/ai/provider.ts (static test enforces); all provider tests green; typecheck clean. Commit: `refactor(08-01): extract rewriteRouterBody with explicit failure logging (WIP-01)` staging ONLY src/ai/provider.ts + tests/provider.test.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Planner LLM-failure fallback parks for human review (WIP-03 + WIP-02 producer half)</name>
  <files>src/plan/planner.ts, tests/planner-fallback.test.ts (new)</files>
  <behavior>
    - formulateImplementationPlan(ticket, { forceAi: true }) with generateText rejecting → { hasAmbiguities: true, questions: ['LLM planning unavailable — human plan review required'], planMarkdown: '', estimatedFiles: [], testStrategy: '', fallbackUsed: true, model: env.API_MODEL } + console.warn called; boilerplate auto-proceed plan GONE
    - generateText resolving { output: validPlan, response: { model: 'router-actual-1' } } → parsed plan + fallbackUsed: false + model: 'router-actual-1'
    - generateText resolving without response.model → model falls back to env.API_MODEL
    - Fallback result passes PlanResultSchema.parse (worker's `if (plan.hasAmbiguities)` branch consumes it unchanged)
    - NODE_ENV=test deterministic branch (no forceAi) unchanged: returns existing shapes, generateText never called, results now additionally carry fallbackUsed: false + model: env.API_MODEL
    - Static caller guard: src/execute/rework-worker.ts contains no 'formulateImplementationPlan'; src/execute/worker.ts routes hasAmbiguities → createPlanCheckpoint
  </behavior>
  <action>
    In src/plan/planner.ts:
    1. Add exported type: `export type PlannerOutcome = PlanResult & { fallbackUsed: boolean; model: string };` and change the signature to `Promise<PlannerOutcome>`.
    2. Wrap the two existing NODE_ENV=test deterministic returns (lines 22-47) as `{ ...PlanResultSchema.parse({...unchanged...}), fallbackUsed: false, model: env.API_MODEL }` — shapes/branches otherwise untouched.
    3. Success path: `return { ...PlanResultSchema.parse(result.output), fallbackUsed: false, model: (result.response?.model as string | undefined) || env.API_MODEL };`
    4. Replace the catch-branch boilerplate plan (lines 81-90) with:
    ```typescript
    } catch (err) {
      console.warn('[planner] LLM plan generation call failed; parking ticket for human plan review:', (err as any)?.message || err);
      return {
        ...PlanResultSchema.parse({
          hasAmbiguities: true,
          questions: ['LLM planning unavailable — human plan review required'],
          planMarkdown: '',
          estimatedFiles: [],
          testStrategy: '',
        }),
        fallbackUsed: true,
        model: env.API_MODEL,
      };
    }
    ```
    Use the em-dash (—) in the question string exactly as written. Do NOT change the prompt text, instructions, or the uncommitted JSON-schema instruction block. No changes to src/execute/worker.ts in this task — its existing `if (plan.hasAmbiguities) { createPlanCheckpoint(...) }` branch (worker.ts:422-430, proven by tests/plan-checkpoint.test.ts:280 ambiguity-flow test) handles the park shape as-is; park + [awaiting-input] tag + 24h ping + 72h Blocked come from the existing checkpoint/watchdog machinery per 08-CONTEXT.

    Create tests/planner-fallback.test.ts using the hoisted-mock pattern:
    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';
    const { generateTextMock } = vi.hoisted(() => ({ generateTextMock: vi.fn() }));
    vi.mock('ai', async (importOriginal) => {
      const mod = await importOriginal<typeof import('ai')>();
      return { ...mod, generateText: generateTextMock };
    });
    import { formulateImplementationPlan } from '../src/plan/planner.js';
    import { PlanResultSchema } from '../src/plan/schema.js';
    import { env } from '../src/config/env.js';
    import { readFileSync } from 'node:fs';
    ```
    Clear the mock + console.warn spy in beforeEach. Implement the behavior cases above (clear ticket fixture: title 'Add endpoint', description 'Returns 200 with JSON', acceptanceCriteria 'GET /x returns 200'). Static caller guard test: readFileSync both worker sources; assert rework-worker has no 'formulateImplementationPlan' and worker.ts contains both 'plan.hasAmbiguities' and 'createPlanCheckpoint'.
  </action>
  <verify>
    <automated>npx vitest run tests/planner-fallback.test.ts tests/planner.test.ts; if ($?) { npx tsc --noEmit }</automated>
  </verify>
  <done>Catch branch returns the park shape with fallbackUsed:true + resolved model; no boilerplate auto-proceed plan exists; existing planner tests green (additive fields only); rework-worker verified planner-free with a permanent guard test. Commit: `fix(08-01): planner LLM-failure fallback parks for human review (WIP-03)` staging ONLY src/plan/planner.ts + tests/planner-fallback.test.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Evaluator returns fallbackUsed + actual model on every path (WIP-02 producer half)</name>
  <files>src/auditor/evaluator.ts, tests/auditor-fallback.test.ts (new)</files>
  <behavior>
    - auditTicketContract(ticket, { forceAi: true }) with generateText rejecting → deterministic DoD rubric result (same passed/reasons/criteria_summary as evaluateDoDDeterministically) + fallbackUsed: true + model: env.API_MODEL + console.warn called
    - generateText resolving { output: validAudit, response: { model: 'm-actual' } } → fallbackUsed: false + model: 'm-actual'
    - generateText resolving without response.model → model: env.API_MODEL
    - NODE_ENV=test deterministic path (no forceAi) → existing rubric result + fallbackUsed: false + model: env.API_MODEL; generateText never called
    - Mock path (opts.mock) → fallbackUsed: false + model: 'mock'
    - Existing callers unaffected: formatL1AuditComment(result) and auditor/worker.ts still compile (structural superset); tests/auditor.test.ts + tests/worker.test.ts stay green (worker still hardcodes model:'gpt-4o' until plan 08-02 — that assertion passes because test-env env.API_MODEL resolves to 'gpt-4o')
  </behavior>
  <action>
    In src/auditor/evaluator.ts:
    1. Add `export type AuditOutcome = AuditResult & { fallbackUsed: boolean; model: string };` and change auditTicketContract's return type to `Promise<AuditOutcome>`.
    2. Mock path (line 142-145): `return { ...AuditResultSchema.parse(mockRes), fallbackUsed: false, model: 'mock' };`
    3. NODE_ENV=test path (line 148-150): `return { ...evaluateDoDDeterministically(ticket), fallbackUsed: false, model: env.API_MODEL };` — rubric logic untouched.
    4. Success path: `return { ...AuditResultSchema.parse(result.output), fallbackUsed: false, model: (result.response?.model as string | undefined) || env.API_MODEL };`
    5. Catch path (keep the existing console.warn): `return { ...evaluateDoDDeterministically(ticket), fallbackUsed: true, model: env.API_MODEL };`
    Do NOT modify evaluateDoDDeterministically, the uncommitted prompt.ts JSON-schema text, or auditor/worker.ts (persistence is plan 08-02 task 1).

    Create tests/auditor-fallback.test.ts with the same vi.hoisted + vi.mock('ai', importOriginal) pattern as task 2. Ticket fixture that deterministically PASSES the rubric (e.g. title 'Add user endpoint', description 'API service returns 200 response', acceptanceCriteria 'Given a user, when GET /users, then returns 200 with JSON body') so the fallback assertion is `passed: true` with rubric reasons; add one failing fixture (description containing 'TBD') asserting `passed: false` through the fallback path. Assert console.warn spy called on the reject path only.
  </action>
  <verify>
    <automated>npx vitest run tests/auditor-fallback.test.ts tests/auditor.test.ts tests/worker.test.ts; if ($?) { npx tsc --noEmit }; if ($?) { npm test 2>&1 | Select-Object -Last 5 }</automated>
  </verify>
  <done>All four return paths carry fallbackUsed + model; full suite (462 + new) green; no caller broken. Commit: `feat(08-01): evaluator records fallbackUsed + actual model (WIP-02)` staging ONLY src/auditor/evaluator.ts + tests/auditor-fallback.test.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator → LLM API (customStreamFetch) | outbound request body rewritten; upstream response untrusted |
| LLM response → pipeline decisions | planner/evaluator output drives gates; failure fallback must not silently downgrade governance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | customStreamFetch body rewrite (malformed/unparseable body) | mitigate | rewriteRouterBody forwards the ORIGINAL body on parse failure + console.warn — never drops or corrupts; object-guard prevents primitive-JSON edge throws; unit-tested failure branch (Task 1) |
| T-08-02 | Repudiation | evaluator/planner LLM-failure fallbacks (AUDIT M7) | mitigate | every return path stamps fallbackUsed + actual/resolved model (Tasks 2-3); persisted into evidence by plan 08-02 |
| T-08-03 | Elevation of Privilege | planner catch branch bypassing the Plan-Q&A human gate | mitigate | fallback now returns hasAmbiguities:true → existing createPlanCheckpoint park + [awaiting-input] + 24h/72h watchdog; boilerplate auto-proceed plan deleted (Task 2) |
| T-08-04 | Information Disclosure | console.warn logging of error messages | accept | warn logs `(err as any)?.message` only — AI SDK/network error messages; no request bodies, keys, or ticket content logged; matches existing WIP logging style |
</threat_model>

<verification>
1. `npm test` → 48+ files, 462 existing + new tests, all green.
2. `npx tsc --noEmit` → clean.
3. `rg "catch\s*\{\s*\}" src/ai/provider.ts` → no match (enforced by static test).
4. `rg "Autonomous execution delegated" src/plan/planner.ts` → no match; `rg "LLM planning unavailable" src/plan/planner.ts` → 1 match.
5. `git log --oneline -3` → 3 atomic commits (provider, planner, evaluator); remaining dirty files: src/auditor/prompt.ts, src/execute/worker.ts, src/auditor/worker.ts territory + untracked src/cli/ (plan 08-02 finishes).
6. No §Done-well invariant touched: no edits to HMAC, dedup, lane-manager, formatters' sanitization, store writes, breakers, withRetry.
</verification>

<success_criteria>
- SC1 (ROADMAP Phase 8 #1) fully satisfied: rewriteRouterBody unit-tested incl. failure branch; failures logged; zero silent `catch {}`.
- SC3 mechanism satisfied: planner LLM failure → hasAmbiguities:true park shape, boilerplate plan removed, schema-valid, caller routing verified + guard-tested.
- SC2 producer half satisfied: fallbackUsed + actual model on every evaluator/planner return path (persistence in 08-02).
- Full suite green, no new deps, no Phase 9 pre-emption.
</success_criteria>

<source_audit>
| Item | Source | Coverage |
|------|--------|----------|
| GOAL: dirty tree completed under phase discipline (fallback mechanics) | ROADMAP Phase 8 | Tasks 1-3 |
| WIP-01 | REQUIREMENTS | Task 1 |
| WIP-02 (evaluator/planner return shapes) | REQUIREMENTS | Tasks 2-3 (persistence → 08-02) |
| WIP-03 | REQUIREMENTS | Task 2 |
| L9 (silent catch {}, unrecorded fallbacks, zero tests) | AUDIT-v2.1 | Tasks 1-3 |
| M7 (fallback downgrades evidence; planner must park) | AUDIT-v2.1 | Tasks 2-3 |
| CONTEXT: keep 9router behavior exactly; forceAiPlanner escape stays; deterministic test branches stay | 08-CONTEXT specifics | Constraints block + Task 1/2/3 actions |
| Deferred (NOT here): cli wire-or-delete fix → Phase 12; prompt XML isolation → Phase 9; other LLM call-site fallback recording → Phase 10+ | 08-CONTEXT deferred | Excluded by design |
</source_audit>

<output>
After completion, create `.planning/phases/08-wip-resolution/08-01-SUMMARY.md`
</output>
