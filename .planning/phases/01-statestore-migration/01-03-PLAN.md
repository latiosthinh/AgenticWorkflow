---
phase: 01-statestore-migration
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/queue/lane-manager.ts
  - src/state/store.ts
  - tests/state-single-writer.test.ts
autonomous: true
requirements:
  - STATE-03

must_haves:
  truths:
    - "Every ticket-state mutation via updateTicketState routes through the per-work-item lane (concurrency: 1)"
    - "Stray mutations outside workItemQueueManager.runInLane(workItemId) are rejected with OffLaneMutationError"
    - "Cross-ticket mutations (lane for workItemId A updating workItemId B) are rejected with OffLaneMutationError"
    - "Ticket file writes are crash-atomic via sibling .tmp staging and Windows-safe replace (unlinkSync then renameSync on win32)"
    - "If a crash occurs before rename, reading the ticket recovers the newest intact sibling .tmp file"
  artifacts:
    - path: "src/queue/lane-manager.ts"
      provides: "AsyncLocalStorage context and runInLane executor"
      exports: ["laneContext", "workItemQueueManager"]
    - path: "src/state/store.ts"
      provides: "OffLaneMutationError and crash-atomic write engine"
      exports: ["OffLaneMutationError", "FileStateStore"]
    - path: "tests/state-single-writer.test.ts"
      provides: "Regression test suite proving invariant enforcement and crash atomicity"
      min_lines: 90
  key_links:
    - from: "src/state/store.ts"
      to: "src/queue/lane-manager.ts"
      via: "laneContext.getStore() inspection in verifyLane"
      pattern: "laneContext\\.getStore\\(\\)"
    - from: "src/state/store.ts"
      to: "node:fs"
      via: "unlinkSync and renameSync in writeCrashAtomicSync"
      pattern: "fs\\.(unlinkSync|renameSync)"
---

<objective>
Enforce the single-writer invariant using Node.js native AsyncLocalStorage bound to the per-work-item concurrency:1 lane, and implement Windows-safe crash-atomic file writes with orphan temp file recovery.

Purpose: Prevent race conditions, off-lane state corruption, and torn writes during crashes or process termination without requiring a relational database.
Output: Enhanced `src/queue/lane-manager.ts`, hardened `src/state/store.ts`, and `tests/state-single-writer.test.ts`.
</objective>

<execution_context>
@$HOME/.config/antigravity/get-shit-done/workflows/execute-plan.md
@$HOME/.config/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-statestore-migration/01-RESEARCH.md
@.planning/phases/01-statestore-migration/01-PATTERNS.md
@src/queue/lane-manager.ts
@src/state/store.ts

<interfaces>
<!-- Contracts to implement and test -->
export class OffLaneMutationError extends Error {
  constructor(workItemId: number, activeLane?: number);
}

export const laneContext: AsyncLocalStorage<{ workItemId: number }>;

export class WorkItemQueueManager {
  getLane(workItemId: number): PQueue;
  runInLane<T>(workItemId: number, fn: () => Promise<T>): Promise<T>;
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AsyncLocalStorage Context and runInLane to WorkItemQueueManager</name>
  <files>src/queue/lane-manager.ts</files>
  <read_first>
    - src/queue/lane-manager.ts
  </read_first>
  <behavior>
    - Export laneContext as AsyncLocalStorage<{ workItemId: number }>.
    - runInLane adds task to queue and runs it within laneContext.run({ workItemId }, fn).
    - Preserves all existing methods: getLane, clearLane, getActiveLaneCount, drainAll.
  </behavior>
  <action>
    Update `src/queue/lane-manager.ts`:
    1. Import `AsyncLocalStorage` from `node:async_hooks`.
    2. Export `const laneContext = new AsyncLocalStorage<{ workItemId: number }>();`.
    3. In `WorkItemQueueManager`, add method:
       ```typescript
       public async runInLane<T>(workItemId: number, fn: () => Promise<T>): Promise<T> {
         const lane = this.getLane(workItemId);
         return lane.add(() => laneContext.run({ workItemId }, fn));
       }
       ```
    4. Keep all existing queue lane management logic intact.
  </action>
  <verify>
    <automated>npx tsx -e "import { laneContext, workItemQueueManager } from './src/queue/lane-manager.js'; workItemQueueManager.runInLane(123, async () => { if (laneContext.getStore()?.workItemId !== 123) throw new Error('Context mismatch'); console.log('Lane context verified'); });"</automated>
  </verify>
  <acceptance_criteria>
    - `laneContext` is exported and tracks active `workItemId`.
    - `runInLane` serializes execution on the dedicated lane with `concurrency: 1`.
  </acceptance_criteria>
  <done>WorkItemQueueManager enhanced with AsyncLocalStorage execution context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce verifyLane in FileStateStore, Implement Crash-Atomic Writes, and Verify Invariant</name>
  <files>src/state/store.ts, tests/state-single-writer.test.ts</files>
  <read_first>
    - src/state/store.ts
    - src/queue/lane-manager.ts
    - src/state/test-harness.ts
  </read_first>
  <behavior>
    - updateTicketState checks verifyLane(workItemId) and throws OffLaneMutationError if outside runInLane or if laneContext.workItemId !== workItemId.
    - Sibling temp files write to .<filename>.tmp.<timestamp>.<rand> in the same directory.
    - Windows NTFS replacement: if process.platform === 'win32' and target exists, fs.unlinkSync(target) before fs.renameSync(temp, target).
    - Crash recovery: if target file does not exist, getTicketState checks for sibling temp files and recovers newest valid file.
    - tests/state-single-writer.test.ts verifies off-lane rejection, lane mismatch rejection, on-lane success, and crash recovery.
  </behavior>
  <action>
    1. Update `src/state/store.ts`:
       - Import `laneContext` from `../queue/lane-manager.js`.
       - Export `class OffLaneMutationError extends Error`:
         ```typescript
         export class OffLaneMutationError extends Error {
           constructor(workItemId: number, activeLane?: number) {
             super(
               `Off-lane mutation rejected: mutation for workItemId ${workItemId} must be executed inside its dedicated lane (active lane: ${activeLane ?? 'none'}).`
             );
             this.name = 'OffLaneMutationError';
           }
         }
         ```
       - Add helper `verifyLane(workItemId: number): void`:
         ```typescript
         function verifyLane(workItemId: number): void {
           const current = laneContext.getStore();
           if (!current || current.workItemId !== workItemId) {
             throw new OffLaneMutationError(workItemId, current?.workItemId);
           }
         }
         ```
       - In `updateTicketState(workItemId: number, ...)`:
         Invoke `verifyLane(workItemId)` at the very beginning before any file reads or state changes.
       - Implement `writeCrashAtomicSync(targetPath: string, content: string): void`:
         - Generate sibling temp path: `path.join(dir, `.${path.basename(targetPath)}.tmp.${Date.now()}.${crypto.randomBytes(4).toString('hex')}`)`.
         - Write content to temp file with `fs.writeFileSync(tempPath, content, 'utf8')`.
         - If `process.platform === 'win32'` and `fs.existsSync(targetPath)`: execute `fs.unlinkSync(targetPath)`.
         - Execute `fs.renameSync(tempPath, targetPath)`.
         - Catch error and clean up temp file if present before rethrowing.
       - In `getTicketState(workItemId)` and `getTicketNotes(workItemId)`:
         If target file does not exist, check for sibling orphan temp files matching `.${workItemId}.md.tmp.*`. If found, sort by mtime descending, rename the newest temp file to target path, and proceed to parse.
    2. Create `tests/state-single-writer.test.ts`:
       - Test 1: Direct call to `updateTicketState(101, ...)` without `runInLane` throws `OffLaneMutationError`.
       - Test 2: Mutation for ticket 202 inside `runInLane(101, ...)` throws `OffLaneMutationError` with mismatch description.
       - Test 3: Mutation for ticket 101 inside `runInLane(101, ...)` succeeds and returns updated state.
       - Test 4: Crash recovery: simulate crash by writing only a sibling `.101.md.tmp.123` file and verify `getTicketState(101)` recovers and parses the state.
       - Test 5: Atomic overwrite: verify existing content is never corrupted if mutator throws before write.
  </action>
  <verify>
    <automated>npx vitest run tests/state-single-writer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `OffLaneMutationError` is thrown on any mutation outside the matching lane.
    - All tests in `tests/state-single-writer.test.ts` pass 100%.
    - Crash-atomic rename operates cleanly on Windows without `EPERM` collisions.
  </acceptance_criteria>
  <done>Single-writer invariant strictly enforced by StateStore API and crash atomicity proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StateStore API ↔ Caller | Off-lane callers (background tasks, unintended direct calls) could overwrite state concurrently |
| OS Process ↔ Filesystem | Process crash or power loss during file write could leave zero-byte or torn files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | StateStore.updateTicketState | mitigate | Enforce verifyLane() via AsyncLocalStorage; throw OffLaneMutationError on context absence/mismatch |
| T-01-08 | Denial of Service | writeCrashAtomicSync | mitigate | Stage write to sibling temp file; unlinkSync then renameSync on Windows; orphan temp recovery on read |
</threat_model>

<verification>
Automated vitest execution:
`npx vitest run tests/state-single-writer.test.ts`
</verification>

<success_criteria>
1. Single-writer invariant is strictly enforced at runtime by `StateStore`.
2. Stray direct writes fail with `OffLaneMutationError`.
3. Crash-atomic writes prevent torn state on Windows.
</success_criteria>

<output>
After completion, create `.planning/phases/01-statestore-migration/01-03-SUMMARY.md`
</output>
