---
phase: 10-security-evidence-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified:
  - src/execute/router.ts
  - src/qa/worker.ts
  - src/deploy/evidence-index.ts
  - src/state/types.ts
  - src/sandbox/runner.ts
  - src/execute/worker.ts
  - src/execute/rework-worker.ts
  - tests/honest-evidence.test.ts
  - tests/repair-secrets.test.ts
autonomous: true
requirements:
  - CORE-04
  - REL-09

must_haves:
  truths:
    - "Router step 4 retrieves real L3 evidence and throws MissingEvidenceError when missing, never fabricating {totalTests:1,passed:1}"
    - "QA worker resolves real commit SHA via git rev-parse HEAD in worktree, never hardcoding 'main'"
    - "Evidence index compiler eliminates hardcoded constants for L2/L4/errorRate/p95 under failClosed: true, requiring real evidence or throwing MissingEvidenceError"
    - "executeRepairLoop passes knownSecrets consistently with QA and smoke runners"
    - "SENSITIVE_VALUE_PATTERN scrubs raw ADO PATs"
  artifacts:
    - path: "src/execute/router.ts"
      provides: "Fail-closed L3 evidence retrieval in step 4"
      contains: "MissingEvidenceError"
    - path: "src/qa/worker.ts"
      provides: "Dynamic git rev-parse HEAD resolution in worktree"
      contains: "rev-parse"
    - path: "src/deploy/evidence-index.ts"
      provides: "Strict evidence compilation without fabricated constants"
    - path: "src/sandbox/runner.ts"
      provides: "getKnownSecrets helper and enhanced SENSITIVE_VALUE_PATTERN"
      contains: "getKnownSecrets"
    - path: "tests/honest-evidence.test.ts"
      provides: "Tests for zero-fabricated-evidence across router, QA, and evidence index"
    - path: "tests/repair-secrets.test.ts"
      provides: "Tests for knownSecrets propagation in executeRepairLoop"
  key_links:
    - from: "src/execute/router.ts"
      to: "src/deploy/evidence-index.ts"
      via: "import { MissingEvidenceError }"
      pattern: "MissingEvidenceError"
    - from: "src/execute/worker.ts"
      to: "src/sandbox/runner.ts"
      via: "import { getKnownSecrets }"
      pattern: "getKnownSecrets"
    - from: "src/execute/rework-worker.ts"
      to: "src/sandbox/runner.ts"
      via: "import { getKnownSecrets }"
      pattern: "getKnownSecrets"
---

<objective>
Eliminate all fabricated evidence defaults across router, QA worker, and evidence index (CORE-04), and pass known secrets into executeRepairLoop for consistent secret scrubbing (REL-09).

Purpose: Guarantee every recorded evidence tier originates from a verified run, preventing phantom pass states and secret leakage during test repair loops.
Output: Fail-closed router step 4, real git rev-parse in QA, honest evidence index compilation, and secret-scrubbed repair loop.
</objective>

<execution_context>
@$HOME/.config/antigravity/get-shit-done/workflows/execute-plan.md
@$HOME/.config/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-security-evidence-hardening/10-CONTEXT.md
@src/execute/router.ts
@src/qa/worker.ts
@src/deploy/evidence-index.ts
@src/state/types.ts
@src/sandbox/runner.ts
@src/execute/worker.ts
@src/execute/rework-worker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zero Fabricated Evidence in Router, QA, and Evidence Index (CORE-04)</name>
  <files>src/execute/router.ts, src/qa/worker.ts, src/deploy/evidence-index.ts, src/state/types.ts, tests/honest-evidence.test.ts</files>
  <action>
1. In `src/execute/router.ts` (Step 4, line 200-217):
   - Retrieve latest L3 evidence:
     `const evidence = (ticketState.l3Evidence && ticketState.l3Evidence.length > 0) ? ticketState.l3Evidence[ticketState.l3Evidence.length - 1] : undefined;`
   - If `!evidence`:
     - Do NOT fallback to `{ suite: 'vitest', totalTests: 1, passed: 1, failed: 0, durationMs: 100 }`.
     - Throw `new MissingEvidenceError('Missing required L3 test evidence for PR creation in step 4', 'L3', workItemId)`.
     - Ensure router catches or propagates this fail-closed (ticket blocked, dedup status `failed`).
2. In `src/qa/worker.ts` (line 65):
   - Replace `let commitSha = 'main'` with real git resolution:
     - Inside `worktreePath`, execute `(await git.raw(['rev-parse', 'HEAD'])).trim()`.
     - If rev-parse fails on worktree, attempt resolving the work item task branch ref.
     - Never hardcode `'main'`. If unresolved, fail closed: flag ticket Blocked with `[qa-harness-error]` and return early.
3. In `src/state/types.ts` and `src/deploy/evidence-index.ts`:
   - Add optional `l2Evidence?: { reviewPassed: boolean; qualityNotes: string } | null` and `l4Evidence?: { securityPassed: boolean; policiesSummary: string } | null` to `TicketState` and `compileL1L7EvidenceIndex` options.
   - In `compileL1L7EvidenceIndex`:
     - When `options?.failClosed === true`:
       - For L6: require real `l6Record.errorRate` and `l6Record.p95LatencyMs`. If missing, throw `MissingEvidenceError` for level `'L6'`. Eliminate hardcoded fallback constants `'0.05%'` and `145`.
       - For L2: require `options?.l2Evidence ?? ticket.l2Evidence`. If missing, throw `MissingEvidenceError` for level `'L2'`.
       - For L4: require `options?.l4Evidence ?? ticket.l4Evidence`. If missing, throw `MissingEvidenceError` for level `'L4'`.
     - When `options?.failClosed !== true`:
       - Default gracefully to `'Pending'` or unverified status instead of fabricated `'PASSED'` / `'0.05%'`.
   - Update `seedCompleteTicket` in `tests/deploy-evidence-index.test.ts` to include valid L2 and L4 records so the baseline test suite remains green.
4. Create `tests/honest-evidence.test.ts`:
   - Test router Step 4 throws `MissingEvidenceError` when ticket has no L3 evidence.
   - Test QA worker retrieves real commit SHA via rev-parse and rejects fallback to `'main'`.
   - Test `compileL1L7EvidenceIndex` with `failClosed: true` throws `MissingEvidenceError` when L2, L4, or L6 metrics are missing.
  </action>
  <verify>
    <automated>npx vitest run tests/honest-evidence.test.ts tests/deploy-evidence-index.test.ts</automated>
  </verify>
  <done>Fabricated evidence defaults removed across router, QA worker, and evidence index compiler; fail-closed MissingEvidenceError enforced.</done>
</task>

<task type="auto">
  <name>Task 2: Known Secrets Scrubbing in Repair Loop & Sandboxes (REL-09)</name>
  <files>src/sandbox/runner.ts, src/execute/worker.ts, src/execute/rework-worker.ts, tests/repair-secrets.test.ts</files>
  <action>
1. In `src/sandbox/runner.ts`:
   - Export `getKnownSecrets(): string[]` helper:
     `return [env.ADO_PAT, env.ADO_WEBHOOK_SECRET, env.API_KEY, env.OPENAI_API_KEY].filter((s): s is string => Boolean(s));`
   - Update `SENSITIVE_VALUE_PATTERN` to also match raw 52-character ADO PATs (e.g. `/(?:ghp_[a-zA-Z0-9]{36}|Bearer\s+[a-zA-Z0-9_\-\.]+|ado-[a-zA-Z0-9]{40,}|[a-zA-Z0-9]{52})/g`).
2. In `src/execute/worker.ts`:
   - Import `getKnownSecrets` from `../sandbox/runner.js`.
   - In `executeRepairLoop({ ... })` call (line 228), pass `knownSecrets: getKnownSecrets()`.
3. In `src/execute/rework-worker.ts`:
   - Import `getKnownSecrets` from `../sandbox/runner.js`.
   - In `executeRepairLoop({ ... })` call (line 266), pass `knownSecrets: getKnownSecrets()`.
4. Create `tests/repair-secrets.test.ts`:
   - Verify `getKnownSecrets()` returns configured environment secrets.
   - Verify `scrubOutput` scrubs raw 52-character ADO PATs and known secret values.
   - Verify `executeRepairLoop` passes `knownSecrets` to `mockTestRunner` / `runCommand` and test diagnostics do not leak secrets.
  </action>
  <verify>
    <automated>npx vitest run tests/repair-secrets.test.ts</automated>
  </verify>
  <done>Known secrets are scrubbed consistently across repair loops, sandboxes, and test diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orchestrator Evidence Generation → ADO / StateStore | State transitions gated by recorded test, policy, and telemetry evidence |
| Test Subprocess Output → ADO Comments / History | Output from test runner and repair loops embedded in error reports and diagnostics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Spoofing / Tampering | src/execute/router.ts, src/deploy/evidence-index.ts | mitigate | Throw MissingEvidenceError when real L2/L3/L4/L6 evidence is absent; fail closed |
| T-10-08 | Information Disclosure | src/execute/worker.ts, src/sandbox/runner.ts | mitigate | Pass getKnownSecrets() to executeRepairLoop and scrub raw PATs from all stdout/stderr |
| T-10-09 | Integrity Violation | src/qa/worker.ts | mitigate | Resolve real git rev-parse HEAD commitSha; fail closed if worktree commit is indeterminate |
</threat_model>

<verification>
Run unit tests for honest evidence and repair secret scrubbing:
`npx vitest run tests/honest-evidence.test.ts tests/repair-secrets.test.ts`
Verify full test suite passes:
`npm test`
</verification>

<success_criteria>
- Router step 4 never fabricates L3 evidence; throws MissingEvidenceError when missing.
- QA worker resolves real commit SHA from worktree via rev-parse HEAD.
- Evidence index compiler throws MissingEvidenceError when L2/L4/errorRate/p95 are missing under failClosed: true.
- executeRepairLoop passes knownSecrets and scrubs raw ADO PATs.
- Full test suite (517+ tests) remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/10-security-evidence-hardening/10-03-SUMMARY.md`
</output>
