---
phase: 10-security-evidence-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/ado/formatter.ts
  - src/ado/work-item.ts
  - src/execute/worker.ts
  - src/execute/rework-worker.ts
  - src/execute/repair.ts
  - tests/worker-comment-sanitization.test.ts
  - tests/git-push-honesty.test.ts
autonomous: true
requirements:
  - SEC-04
  - SEC-05

must_haves:
  truths:
    - "Every ADO comment posted by execute/rework workers passes through a sanitizing formatter and carries the <!-- [automated-agent] --> loop shield"
    - "Production git push failures fail closed (transition ticket to Blocked, record dedup failed, post alert comment)"
    - "Test environment git push failures are tolerated for offline testing"
  artifacts:
    - path: "src/ado/formatter.ts"
      provides: "formatWorkerAlertComment helper"
      contains: "formatWorkerAlertComment"
    - path: "src/ado/work-item.ts"
      provides: "Sanitized and loop-shielded flagTicketBlocked"
    - path: "src/execute/worker.ts"
      provides: "Sanitized comments and fail-closed push error handling"
    - path: "src/execute/rework-worker.ts"
      provides: "Sanitized comments and fail-closed push error handling"
    - path: "src/execute/repair.ts"
      provides: "Fail-closed push error handling in production"
    - path: "tests/worker-comment-sanitization.test.ts"
      provides: "Comment inventory test verifying all comments are sanitized and shielded"
    - path: "tests/git-push-honesty.test.ts"
      provides: "Tests verifying fail-closed behavior on push failure under production"
  key_links:
    - from: "src/execute/worker.ts"
      to: "src/ado/formatter.ts"
      via: "formatWorkerAlertComment"
      pattern: "formatWorkerAlertComment"
    - from: "src/execute/rework-worker.ts"
      to: "src/ado/formatter.ts"
      via: "formatWorkerAlertComment"
      pattern: "formatWorkerAlertComment"
---

<objective>
Enforce 100% comment sanitization with loop-shield markers across execute and rework workers (SEC-04) and ensure git push failures fail closed in production (SEC-05).

Purpose: Prevent recursive webhook loops from unsanitized/unshielded bot comments and prevent tickets with unpushed branches from silently advancing to Dev Done.
Output: Sanitized and shielded worker comments, centralized alert formatting, and fail-closed production push error handling.
</objective>

<execution_context>
@$HOME/.config/antigravity/get-shit-done/workflows/execute-plan.md
@$HOME/.config/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-security-evidence-hardening/10-CONTEXT.md
@src/ado/formatter.ts
@src/ado/work-item.ts
@src/execute/worker.ts
@src/execute/rework-worker.ts
@src/execute/repair.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 100% Comment Sanitization & Loop-Shielding in Workers (SEC-04)</name>
  <files>src/ado/formatter.ts, src/ado/work-item.ts, src/execute/worker.ts, src/execute/rework-worker.ts, tests/worker-comment-sanitization.test.ts</files>
  <action>
1. In `src/ado/formatter.ts`, add and export `formatWorkerAlertComment(title: string, message: string, details?: string): string`:
   - Builds markdown or structured HTML with title, message, and optional details (e.g. code/pre block).
   - Sanitizes output via `sanitizeHtml` allowing standard safe tags (`h3`, `p`, `pre`, `code`, `strong`, `ul`, `li`).
   - Guarantees trailing `\n<!-- [automated-agent] -->`.
2. In `src/ado/work-item.ts`:
   - In `flagTicketBlocked(workItemId, htmlComment, type)`:
     - Ensure `htmlComment` is sanitized via `sanitizeHtml`.
     - Ensure `htmlComment` contains `<!-- [automated-agent] -->`; if not present, append it before adding to patch document.
3. In `src/execute/worker.ts`:
   - Refactor all inline ADO comments to route through `formatWorkerAlertComment` or sanitizing formatter with loop shield:
     - Disallowed packages contract-conflict (`diffStat.unauthorizedPackages`)
     - Modified test files contract-conflict (`diffStat.modifiedTestFiles`)
     - OpenCode error (`runnerResult.error`)
     - Repair budget exhausted (`repairResult.diagnostics`)
     - Plan review auto-lock comment (line 479)
4. In `src/execute/rework-worker.ts`:
   - Refactor all inline ADO comments to route through `formatWorkerAlertComment` with loop shield:
     - OpenCode rework failed / timed out (`runRes.error`)
     - Diff ceiling exceeded (`cumulativeDiff.totalLoc`)
     - Disallowed package dependencies (`unauthorizedPackages`)
     - Protected test files modified (`immutability.violations`)
     - New test file lacks valid assertions (`newTest`)
     - Repair budget exhausted (`repairResult.diagnostics`)
5. Create `tests/worker-comment-sanitization.test.ts`:
   - Enumerate all worker alert comment generators and verify each outputs valid HTML sanitized of `<script>` / malicious attributes.
   - Assert each comment carries `<!-- [automated-agent] -->`.
   - Assert thread filter (`isAutomatedAgentComment` or `<!-- [automated-agent] -->`) reliably identifies every worker comment.
  </action>
  <verify>
    <automated>npx vitest run tests/worker-comment-sanitization.test.ts</automated>
  </verify>
  <done>Zero unshielded or unsanitized comments exist in execute/rework workers; all comments carry loop-shield markers.</done>
</task>

<task type="auto">
  <name>Task 2: Production Git Push Failure Honesty (SEC-05)</name>
  <files>src/execute/worker.ts, src/execute/rework-worker.ts, src/execute/repair.ts, tests/git-push-honesty.test.ts</files>
  <action>
1. In `src/execute/worker.ts` (around line 288):
   - Wrap `await git.push('origin', worktreeResult.branchName)` in try/catch:
     - If `env.NODE_ENV === 'test'`, log warning and proceed (preserve offline test suite execution).
     - Otherwise (production, staging, development):
       - Log error: `[worker] Git push to origin failed: ${err.message}`.
       - Call `await flagTicketBlocked(workItem.id, formatWorkerAlertComment('[Push Failed] Remote push to origin failed', err.message), 'contract-conflict')`.
       - Record dedup status `failed`.
       - Clean up worktree and return early (do not transition to Dev Done).
2. In `src/execute/rework-worker.ts` (around line 295):
   - Apply the same fail-closed logic on `await git.push('origin', worktreeResult.branchName)` failure:
     - Under `NODE_ENV === 'test'`, log warning and proceed.
     - Under `NODE_ENV !== 'test'`, flag ticket as Blocked with alert comment, record dedup status `failed`, clean up worktree, and return early.
3. In `src/execute/repair.ts` (around line 100):
   - Wrap `await options.git.push('origin', wipBranch)`:
     - Under `NODE_ENV === 'test'`, log warning and proceed.
     - Under `NODE_ENV !== 'test'`, record push failure in return diagnostics (`diagnostics += '\nRemote push to WIP branch failed'`) and ensure failure is visible.
4. Create `tests/git-push-honesty.test.ts`:
   - Mock git push failure under `NODE_ENV='production'`: verify ticket is flagged Blocked, dedup status is set to `failed`, alert comment posted, and worktree cleaned up.
   - Mock git push failure under `NODE_ENV='test'`: verify push error is caught and execution proceeds to Dev Done for offline unit testing.
  </action>
  <verify>
    <automated>npx vitest run tests/git-push-honesty.test.ts</automated>
  </verify>
  <done>Production push failure transitions ticket to Blocked and marks dedup failed; test-environment push failure continues to be tolerated for offline testing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker Diagnostic Comments → ADO History | Raw diagnostics and subprocess error strings posted into ticket history |
| Worker Git Client → Azure Repos Remote | Push of task and WIP branches across network to remote repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Repudiation / Denial of Service | src/execute/worker.ts, src/execute/rework-worker.ts | mitigate | Append `<!-- [automated-agent] -->` loop shield to all comments so webhook filters drop bot echoes |
| T-10-05 | Tampering / XSS | src/execute/worker.ts, src/ado/work-item.ts | mitigate | Sanitize all error strings and diagnostics via sanitizeHtml in formatWorkerAlertComment |
| T-10-06 | Integrity Violation | src/execute/worker.ts, src/execute/rework-worker.ts | mitigate | Fail closed on push failure in production: block ticket, do not transition to Dev Done |
</threat_model>

<verification>
Run unit tests for comment sanitization and push honesty:
`npx vitest run tests/worker-comment-sanitization.test.ts tests/git-push-honesty.test.ts`
Verify full suite passes:
`npm test`
</verification>

<success_criteria>
- 100% of inline comments in execute and rework workers use sanitizing formatters and loop shield.
- Production git push failure blocks the ticket and records dedup failed.
- Test environment offline push error tolerance preserved.
- Full test suite (517+ tests) remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/10-security-evidence-hardening/10-02-SUMMARY.md`
</output>
