---
phase: 11-reliability-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scope/gate.ts
  - src/config/env.ts
  - src/auditor/evaluator.ts
  - src/plan/planner.ts
  - src/ai/provider.ts
  - src/ado/client.ts
  - tests/tag-wipe-guard.test.ts
  - tests/timeouts.test.ts
autonomous: true
requirements:
  - REL-01
  - REL-02
must_haves:
  truths:
    - "An ADO tag-fetch failure aborts or retries the operation — no Replace patch can ever wipe ticket tags with an empty or partial set"
    - "Every LLM call carries an abortSignal timeout governed by LLM_TIMEOUT_MS"
    - "ADO REST calls and customStreamFetch carry request timeouts governed by ADO_TIMEOUT_MS and LLM_TIMEOUT_MS"
  artifacts:
    - path: "src/scope/gate.ts"
      provides: "Tag fetch retry and abort on failure without empty tag fallback"
    - path: "src/config/env.ts"
      provides: "LLM_TIMEOUT_MS and ADO_TIMEOUT_MS configuration"
    - path: "src/auditor/evaluator.ts"
      provides: "generateText call configured with AbortSignal.timeout(env.LLM_TIMEOUT_MS)"
    - path: "src/plan/planner.ts"
      provides: "generateText call configured with AbortSignal.timeout(env.LLM_TIMEOUT_MS)"
    - path: "src/ai/provider.ts"
      provides: "customStreamFetch enforcing timeout signal"
    - path: "src/ado/client.ts"
      provides: "withRetry wrapping operations with ADO_TIMEOUT_MS"
    - path: "tests/tag-wipe-guard.test.ts"
      provides: "Automated regression tests proving tag fetch failures abort operation without wiping tags"
    - path: "tests/timeouts.test.ts"
      provides: "Automated regression tests verifying LLM and ADO call timeouts"
  key_links:
    - from: "src/scope/gate.ts"
      to: "src/ado/client.ts"
      via: "withRetry(() => adoClient.getWorkItem(workItemId))"
      pattern: "withRetry.*getWorkItem"
    - from: "src/auditor/evaluator.ts"
      to: "src/config/env.ts"
      via: "AbortSignal.timeout(env.LLM_TIMEOUT_MS)"
      pattern: "AbortSignal\\.timeout.*LLM_TIMEOUT_MS"
    - from: "src/plan/planner.ts"
      to: "src/config/env.ts"
      via: "AbortSignal.timeout(env.LLM_TIMEOUT_MS)"
      pattern: "AbortSignal\\.timeout.*LLM_TIMEOUT_MS"
    - from: "src/ai/provider.ts"
      to: "src/config/env.ts"
      via: "LLM_TIMEOUT_MS signal in customStreamFetch"
      pattern: "LLM_TIMEOUT_MS"
    - from: "src/ado/client.ts"
      to: "src/config/env.ts"
      via: "ADO_TIMEOUT_MS in withRetry"
      pattern: "ADO_TIMEOUT_MS"
---

<objective>
Harden tag patch operations against tag wiping on transient ADO fetch errors (REL-01) and enforce timeout signals across all LLM and ADO REST operations (REL-02).

Purpose: Prevent transient network blips from wiping existing tags on work items and eliminate un-bounded hanging requests across LLM and ADO endpoints.
Output: Hardened `src/scope/gate.ts`, timeout configs in `src/config/env.ts`, timeout signals in `evaluator.ts`, `planner.ts`, `provider.ts`, and `client.ts`, verified by unit tests.
</objective>

<execution_context>
@$HOME/.config/antigravity/get-shit-done/workflows/execute-plan.md
@$HOME/.config/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reliability-hardening/11-CONTEXT.md
@src/scope/gate.ts
@src/config/env.ts
@src/auditor/evaluator.ts
@src/plan/planner.ts
@src/ai/provider.ts
@src/ado/client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Eliminate tag wipe on ADO fetch failure in scope gate (REL-01)</name>
  <files>src/scope/gate.ts, tests/tag-wipe-guard.test.ts</files>
  <behavior>
    - Test 1: In handleScopeReset, when currentTags is undefined and getWorkItem throws, withRetry retries and if all retries fail, it throws an error instead of replacing tags with empty string
    - Test 2: In handleScopeApproval, when currentTags is undefined and getWorkItem fails, it throws error and halts without applying buildScopeApprovedPatch with empty tags
    - Test 3: In handleScopeRejection, when currentTags is undefined and getWorkItem fails, it throws error and halts without applying buildScopeEscalationPatch with empty tags
    - Test 4: When getWorkItem succeeds on retry, existing tags are preserved and updated correctly
  </behavior>
  <action>
    In `src/scope/gate.ts` (lines 131-137, 249-257, 276-283):
    1. Remove all empty `tags = ''` catch fallback blocks.
    2. Import `withRetry` from `../ado/client.js`.
    3. Replace direct `getWorkItem` fetch with:
       ```typescript
       const item = await withRetry(() => adoClient.getWorkItem(workItemId));
       tags = item.fields?.['System.Tags'] || '';
       ```
    4. If `withRetry` throws (all retries exhausted), allow the error to bubble up or throw an explicit Error (`Failed to fetch tags for work item #${workItemId}; aborting tag patch to prevent tag wipe`). Do NOT catch and fallback to empty string `''`.
    5. Write comprehensive unit tests in `tests/tag-wipe-guard.test.ts` verifying that ADO fetch failures abort the operation and never emit patches with wiped tags.
  </action>
  <verify>
    <automated>npx vitest run tests/tag-wipe-guard.test.ts</automated>
  </verify>
  <done>Tag fetch failure in scope gate retries via withRetry and aborts with an error when exhausted; zero code paths replace tags with an empty fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce LLM & ADO request timeouts via AbortSignal (REL-02)</name>
  <files>src/config/env.ts, src/auditor/evaluator.ts, src/plan/planner.ts, src/ai/provider.ts, src/ado/client.ts, tests/timeouts.test.ts</files>
  <behavior>
    - Test 1: EnvSchema defines LLM_TIMEOUT_MS (default 60_000, max 180_000) and ADO_TIMEOUT_MS (default 30_000)
    - Test 2: evaluateTicketContract passes AbortSignal.timeout(env.LLM_TIMEOUT_MS) to generateText
    - Test 3: generateImplementationPlan passes AbortSignal.timeout(env.LLM_TIMEOUT_MS) to generateText
    - Test 4: customStreamFetch enforces timeout signal using env.LLM_TIMEOUT_MS, aborting fetch when signal triggers
    - Test 5: withRetry wraps operations with ADO_TIMEOUT_MS, rejecting with ETIMEDOUT when operation hangs beyond timeout
  </behavior>
  <action>
    1. In `src/config/env.ts`:
       - Add `LLM_TIMEOUT_MS: z.coerce.number().min(1000).max(180_000).default(60_000)`
       - Add `ADO_TIMEOUT_MS: z.coerce.number().min(1000).max(120_000).default(30_000)`
    2. In `src/auditor/evaluator.ts`:
       - Pass `abortSignal: AbortSignal.timeout(env.LLM_TIMEOUT_MS)` into `generateText` in `evaluateTicketContract`.
    3. In `src/plan/planner.ts`:
       - Pass `abortSignal: AbortSignal.timeout(env.LLM_TIMEOUT_MS)` into `generateText` in `generateImplementationPlan`.
    4. In `src/ai/provider.ts`:
       - In `customStreamFetch`, combine caller's `init?.signal` with `AbortSignal.timeout(env.LLM_TIMEOUT_MS)` (using `AbortSignal.any([init.signal, timeoutSignal])` or listener bridge if `AbortSignal.any` is available in Node 24).
    5. In `src/ado/client.ts`:
       - In `withRetry`, enforce `timeoutMs = env.ADO_TIMEOUT_MS` on each attempt using `Promise.race`:
         ```typescript
         const timeoutPromise = new Promise<never>((_, reject) => {
           const timer = setTimeout(() => {
             const timeoutErr: any = new Error(`ADO request timed out after ${env.ADO_TIMEOUT_MS}ms`);
             timeoutErr.code = 'ETIMEDOUT';
             reject(timeoutErr);
           }, env.ADO_TIMEOUT_MS);
           timer.unref?.();
         });
         return await Promise.race([fn(), timeoutPromise]);
         ```
    6. Write unit tests in `tests/timeouts.test.ts` proving timeout cancellation behavior on hung LLM and ADO calls.
  </action>
  <verify>
    <automated>npx vitest run tests/timeouts.test.ts</automated>
  </verify>
  <done>All LLM calls carry LLM_TIMEOUT_MS abortSignal; ADO calls time out with ETIMEDOUT after ADO_TIMEOUT_MS and retry appropriately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADO REST API | External cloud service boundary with potential transient network failure or latency spikes |
| LLM Provider API | External LLM service endpoint with potential hang, delay, or stream stall |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Denial of Service | `src/auditor/evaluator.ts`, `src/plan/planner.ts`, `src/ai/provider.ts` | mitigate | Pass `AbortSignal.timeout(env.LLM_TIMEOUT_MS)` to abort hung LLM requests |
| T-11-02 | Denial of Service | `src/ado/client.ts` | mitigate | Enforce `env.ADO_TIMEOUT_MS` timeout on each attempt in `withRetry` with ETIMEDOUT classification |
| T-11-03 | Tampering | `src/scope/gate.ts` | mitigate | Fetch tags via `withRetry` and throw/abort on failure; never fallback to `tags=''` which erases ticket tags |
</threat_model>

<verification>
- `npx vitest run tests/tag-wipe-guard.test.ts` passes.
- `npx vitest run tests/timeouts.test.ts` passes.
- `npm test` passes with all existing tests intact.
</verification>

<success_criteria>
- ADO tag fetch failures retry and fail closed without emitting tag-wiping Replace patches.
- All LLM and ADO requests carry bounded timeouts.
- Full test suite remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-reliability-hardening/11-01-SUMMARY.md`
</output>
