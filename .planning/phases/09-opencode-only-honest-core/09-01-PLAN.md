---
phase: 09-opencode-only-honest-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/execute/worker.ts
  - src/execute/coder.ts
  - src/config/env.ts
  - src/mcp/registry.ts
  - src/mcp/server.ts
  - src/mcp/types.ts
  - src/mcp/tools/common.ts
  - src/mcp/tools/frontend.ts
  - src/mcp/tools/backend.ts
  - src/mcp/tools/infra.ts
  - tests/mcp-registry.test.ts
  - tests/diff-ceiling.test.ts
  - tests/provider.test.ts
  - package.json
autonomous: true
requirements: [CORE-01, CORE-03]

must_haves:
  truths:
    - "Boot fails fast with actionable error when LOCAL_AGENT_TYPE unset or not 'opencode'"
    - "Boot fails fast when OPENCODE_BIN is unresolvable on PATH"
    - "No built-in coding path exists in src/execute/ — no route can record L3/Dev Done with zero edits"
    - "No src/mcp/ directory exists; no MCP deps in package.json"
    - "createCoderTools is deleted; commitImplementation preserved and importable"
  artifacts:
    - path: "src/config/env.ts"
      provides: "Fail-fast LOCAL_AGENT_TYPE=opencode + OPENCODE_BIN validation"
      contains: "opencode"
    - path: "tests/env-boot-validation.test.ts"
      provides: "Boot validation tests for required opencode config"
  key_links:
    - from: "src/execute/worker.ts"
      to: "src/execute/coder.ts"
      via: "commitImplementation import (createCoderTools gone, createDynamicMcpTools gone)"
      pattern: "commitImplementation"
---

<objective>
Delete the built-in no-op coding path and MCP dead-wire subsystem; make opencode required at boot.

Purpose: Close H5 (built-in agent codes nothing) and H7 (MCP dead-wired) — after this plan, `LOCAL_AGENT_TYPE=opencode` is the only valid config, `OPENCODE_BIN` must resolve, the entire `src/mcp/` tree + MCP deps are gone, and `createCoderTools` is deleted (per CORE-01). `commitImplementation` stays in `coder.ts` (worker.ts still uses it).
Output: Cleaned worker.ts, pruned coder.ts, validated env.ts, deleted src/mcp/, updated package.json, updated/deleted tests.
</objective>

<execution_context>
@.planning/phases/09-opencode-only-honest-core/09-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/config/env.ts (CURRENT — will be modified):
```typescript
LOCAL_AGENT_TYPE: z.enum(['opencode', 'built-in']).default('built-in'),  // → must become required, opencode-only
OPENCODE_BIN: z.string().default('opencode'),  // → must become required with resolvability check
```

From src/execute/worker.ts (lines to remove):
```typescript
import { createDynamicMcpTools } from '../mcp/registry.js';  // line 20 — DELETE
import { commitImplementation } from './coder.js';  // line 36 — KEEP
// lines 376, 392-401 (mcpSession creation), 447-448 (mcpSession close), 481-482 (mcpSession close), 497-498 (mcpSession close) — all DELETE
```

From src/execute/coder.ts:
```typescript
export function createCoderTools(worktreePath: string) { ... }  // lines 39-83 — DELETE
export async function commitImplementation(...) { ... }  // lines 88-98 — KEEP
```

From tests/diff-ceiling.test.ts:
```typescript
import { createCoderTools, commitImplementation } from '../src/execute/coder.js';  // line 11-14 — remove createCoderTools
// lines 157-232 describe('Bounded Coder Tools...') — 3 tests DELETE (test createCoderTools only)
// line 234+ describe('commits implementation...') — KEEP (tests commitImplementation)
```

From tests/provider.test.ts:
```typescript
expect(parsed.LOCAL_AGENT_TYPE).toBe('built-in');  // line 43 — must update test
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete MCP subsystem + deps (CORE-03)</name>
  <files>src/mcp/registry.ts, src/mcp/server.ts, src/mcp/types.ts, src/mcp/tools/common.ts, src/mcp/tools/frontend.ts, src/mcp/tools/backend.ts, src/mcp/tools/infra.ts, tests/mcp-registry.test.ts, src/execute/worker.ts, package.json</files>
  <action>
1. Delete the entire `src/mcp/` directory tree (registry.ts, server.ts, types.ts, tools/{common,frontend,backend,infra}.ts).
2. Delete `tests/mcp-registry.test.ts`.
3. In `src/execute/worker.ts`:
   - Remove `import { createDynamicMcpTools } from '../mcp/registry.js';` (line 20).
   - Remove the `mcpSession` variable declaration (line 376: `let mcpSession: { close: () => Promise<void> } | undefined;`).
   - Remove the MCP session creation block (lines 386-401: tag parsing + `mcpSession = await createDynamicMcpTools(...)`).
   - Remove all `mcpSession.close()` calls and cleanup (`await mcpSession.close()` at lines ~447-448, ~481-482) and the catch cleanup at lines ~497-498 (`if (mcpSession) { await mcpSession.close().catch(() => {}); }`).
   - Keep the `tags` parsing if used by the planner call (lines 387-390 build tags used at line 423: `formulateImplementationPlan({...tags})`) — but remove the MCP-specific usage.
4. In `package.json`: remove `"@ai-sdk/mcp"` and `"@modelcontextprotocol/sdk"` from dependencies.
5. Run `npm install` to update lockfile.

Per D-MCP-Resolution (CONTEXT.md): "Delete the entire src/mcp/ tree... Remove @ai-sdk/mcp and @modelcontextprotocol/sdk... Remove all MCP references from worker.ts."
  </action>
  <verify>
    <automated>npx tsc --noEmit; if ($?) { node -e "const p=require('./package.json'); if(p.dependencies['@ai-sdk/mcp']||p.dependencies['@modelcontextprotocol/sdk']){process.exit(1)}" }; if ($?) { node -e "const fs=require('fs'); if(fs.existsSync('src/mcp')){process.exit(1)}" }; if ($?) { node -e "const fs=require('fs'); if(fs.existsSync('tests/mcp-registry.test.ts')){process.exit(1)}" }</automated>
  </verify>
  <done>src/mcp/ tree gone, MCP deps gone from package.json, worker.ts compiles without MCP imports, no mcp-registry.test.ts</done>
</task>

<task type="auto">
  <name>Task 2: Delete built-in path + require opencode at boot (CORE-01)</name>
  <files>src/config/env.ts, src/execute/coder.ts, src/execute/worker.ts, src/execute/rework-worker.ts, tests/diff-ceiling.test.ts, tests/provider.test.ts, tests/env-boot-validation.test.ts</files>
  <action>
1. **env.ts** — per D-Built-in-Path-Deletion (CONTEXT.md):
   - Change `LOCAL_AGENT_TYPE` from `z.enum(['opencode', 'built-in']).default('built-in')` → `z.literal('opencode')` (no default — required, must be 'opencode'). Boot fails fast if unset or wrong value.
   - Change `OPENCODE_BIN` from `z.string().default('opencode')` → `z.string().min(1)` with a `.refine()` that checks resolvability: use `child_process.execFileSync('where', [val])` on win32 or `child_process.execFileSync('which', [val])` on posix, wrapped in try/catch. Refine message: `'OPENCODE_BIN not found on PATH — install opencode or set OPENCODE_BIN to absolute path'`. The `which`/`where` check only runs when NODE_ENV is NOT 'test' (skip for CI where opencode may not be installed but tests mock the runner).

2. **coder.ts** — delete `createCoderTools` function (lines 39-83) and its associated `assertInsideWorktree` helper (lines 10-34) and the `tool`/`z` imports that only `createCoderTools` used. Keep `commitImplementation` (lines 88-98) and its `SimpleGit` import. The file should export only `commitImplementation`. Also remove unused imports: `tool` from 'ai', `fs`, `path`, `normalizePath` — ONLY if `commitImplementation` does not use them (it doesn't).

3. **worker.ts** — the `env.LOCAL_AGENT_TYPE === 'opencode'` conditional at line 84 is now always true. Simplify: remove the `if (env.LOCAL_AGENT_TYPE === 'opencode' && ...)` branch wrapper; the opencode path IS the only code path. The `else` case at the top of `runExecutionPipeline` (line 82 `if (options?.mockCodeEdit)`) stays — it's the mock seam.
   Similarly, line 406 `if (env.LOCAL_AGENT_TYPE === 'opencode' && env.NODE_ENV !== 'test' && !options?.forceAiPlanner)` — remove the `env.LOCAL_AGENT_TYPE === 'opencode'` part (always true now). Keep the `env.NODE_ENV !== 'test' && !options?.forceAiPlanner` condition.

4. **rework-worker.ts** — same pattern: line 130 `env.LOCAL_AGENT_TYPE === 'opencode'` check is now always true, simplify the conditional.

5. **tests/diff-ceiling.test.ts** — remove the `createCoderTools` import and the entire `describe('Bounded Coder Tools and Conventional Commit', ...)` block EXCEPT the last test `'commits implementation using conventional format'` which tests `commitImplementation`. Update that test's import to `import { commitImplementation } from '../src/execute/coder.js';` (no `createCoderTools`).

6. **tests/provider.test.ts** — line 43: change `expect(parsed.LOCAL_AGENT_TYPE).toBe('built-in')` to match the new schema (literal 'opencode' required, no default). The test that checks defaults needs updating: when LOCAL_AGENT_TYPE is not provided, parsing should now THROW (or the test should set it to 'opencode' and verify). Update the test name from "provides default values for ... LOCAL_AGENT_TYPE" to reflect the new required field behavior.

7. **tests/env-boot-validation.test.ts** (NEW) — create tests:
   - `'fails fast when LOCAL_AGENT_TYPE is unset'` — parse env without LOCAL_AGENT_TYPE → ZodError.
   - `'fails fast when LOCAL_AGENT_TYPE is built-in'` — parse with LOCAL_AGENT_TYPE='built-in' → ZodError.
   - `'accepts LOCAL_AGENT_TYPE=opencode'` — parse succeeds.
   - `'fails fast when OPENCODE_BIN is empty'` — parse with OPENCODE_BIN='' → ZodError.

Per D-Built-in-Path-Deletion: "Delete all LOCAL_AGENT_TYPE === 'built-in' branches... make LOCAL_AGENT_TYPE required, validate it equals 'opencode'... Add OPENCODE_BIN required with resolvability check."
  </action>
  <verify>
    <automated>npx tsc --noEmit; if ($?) { npx vitest run tests/env-boot-validation.test.ts tests/diff-ceiling.test.ts tests/provider.test.ts --reporter=verbose 2>&1 | Select-String "pass|fail|Tests" }</automated>
  </verify>
  <done>LOCAL_AGENT_TYPE='opencode' required (boot fails fast otherwise); OPENCODE_BIN required with PATH check; createCoderTools deleted; no built-in branches in worker.ts/rework-worker.ts; tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → boot | Config validation ensures only known-good agent type at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | env.ts LOCAL_AGENT_TYPE | mitigate | Zod `z.literal('opencode')` rejects all other values at parse time |
| T-09-02 | Tampering | coder.ts createCoderTools | mitigate | Function deleted entirely — no LLM file-manipulation tools remain in codebase |
| T-09-03 | Information Disclosure | MCP tools common.ts run_test | mitigate | Entire src/mcp/ tree deleted; no unrestricted command execution path remains |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `src/mcp/` directory does not exist
- `tests/mcp-registry.test.ts` does not exist
- `package.json` has no `@ai-sdk/mcp` or `@modelcontextprotocol/sdk`
- `grep -r "createCoderTools" src/` returns 0 matches
- `grep -r "built-in" src/config/env.ts` returns 0 matches
- `grep -r "createDynamicMcpTools" src/` returns 0 matches
- New boot validation tests pass
</verification>

<success_criteria>
1. Boot with LOCAL_AGENT_TYPE unset → ZodError with actionable message
2. Boot with LOCAL_AGENT_TYPE='built-in' → ZodError
3. No `src/mcp/` directory; no MCP deps
4. `createCoderTools` grep returns 0 in src/ and tests/
5. worker.ts and rework-worker.ts have no `=== 'built-in'` or `=== 'opencode'` conditionals (the opencode path is unconditional)
6. Full test suite passes (minus deleted mcp-registry.test.ts + 3 createCoderTools tests; plus new boot validation tests)
</success_criteria>

<output>
After completion, create `.planning/phases/09-opencode-only-honest-core/09-01-SUMMARY.md`
</output>
