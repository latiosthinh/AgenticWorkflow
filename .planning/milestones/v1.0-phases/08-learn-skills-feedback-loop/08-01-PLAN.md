---
phase: 08-learn-skills-feedback-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/db/index.ts
  - src/learn/types.ts
  - src/learn/harvester.ts
  - src/learn/prompt.ts
  - src/learn/generator.ts
  - tests/learn-generator.test.ts
autonomous: true
requirements:
  - LRN-01
must_haves:
  truths:
    - "SQLite schema contains skillsPrs table tracking learned skills and PR links"
    - "Lifecycle harvester aggregates rework cycles, review comments, self-repair attempts, and telemetry metrics"
    - "Prompt generator isolates all untrusted ticket inputs in <learning_source_context> tags with meta-instruction override denial"
    - "Skill generator produces valid SKILL.md markdown with frontmatter, Overview, Patterns, and Pitfalls"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "skillsPrs table definition"
      contains: "skillsPrs"
    - path: "src/learn/harvester.ts"
      provides: "harvestTicketLifecycleData function"
      exports: ["harvestTicketLifecycleData"]
    - path: "src/learn/prompt.ts"
      provides: "buildSkillLearningPrompt function"
      exports: ["buildSkillLearningPrompt"]
    - path: "src/learn/generator.ts"
      provides: "generateSkillFromLifecycle function"
      exports: ["generateSkillFromLifecycle"]
    - path: "tests/learn-generator.test.ts"
      provides: "Unit tests for lifecycle harvesting, prompt isolation, and skill generation"
      min_lines: 80
  key_links:
    - from: "src/learn/harvester.ts"
      to: "src/db/schema.ts"
      via: "reworkCycles, l3Evidence, telemetryEvaluations queries"
      pattern: "db\\.select"
---

<objective>
Implement SQLite schema for skills PRs, build the lifecycle data harvester, prompt-injection resistant prompt generator, and SKILL.md synthesis engine.

Purpose: Fulfills LRN-01 by extracting reusable technical patterns and postmortems from completed ticket lifecycles while defending against prompt injection.
Output: Updated `src/db/schema.ts`, updated `src/db/index.ts`, new `src/learn/types.ts`, `src/learn/harvester.ts`, `src/learn/prompt.ts`, `src/learn/generator.ts`, and test suite `tests/learn-generator.test.ts`.
</objective>

<execution_context>
@$HOME/.config/antigravity/get-shit-done/workflows/execute-plan.md
@$HOME/.config/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Projects/AgenticWorkflow/.planning/PROJECT.md
@D:/Projects/AgenticWorkflow/.planning/ROADMAP.md
@D:/Projects/AgenticWorkflow/.planning/REQUIREMENTS.md
@D:/Projects/AgenticWorkflow/.planning/phases/08-learn-skills-feedback-loop/08-CONTEXT.md
@D:/Projects/AgenticWorkflow/.planning/phases/08-learn-skills-feedback-loop/08-RESEARCH.md
@D:/Projects/AgenticWorkflow/src/db/schema.ts
@D:/Projects/AgenticWorkflow/src/auditor/prompt.ts

<threat_model>
ASVS L1 compliance. All untrusted ticket descriptions, review comments, and error traces are enclosed in `<learning_source_context>` XML tags with explicit instructions denying meta-directive overrides. This prevents malicious prompts from poisoning persistent skills.
</threat_model>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skillsPrs table to SQLite schema</name>
  <files>src/db/schema.ts, src/db/index.ts</files>
  <read_first>
    src/db/schema.ts,
    src/db/index.ts
  </read_first>
  <action>
    Add `skillsPrs` table to `src/db/schema.ts` and `src/db/index.ts`:
    - `id`: `integer('id').primaryKey({ autoIncrement: true })`
    - `workItemId`: `integer('work_item_id').notNull()`
    - `skillName`: `text('skill_name').notNull()`
    - `branchName`: `text('branch_name').notNull()`
    - `pullRequestId`: `integer('pull_request_id')`
    - `prUrl`: `text('pr_url')`
    - `status`: `text('status', { enum: ['pending_review', 'merged', 'closed'] }).notNull().default('pending_review')`
    - `summary`: `text('summary').notNull()`
    - `createdAt`: `integer('created_at', { mode: 'timestamp' }).notNull().$defaultFn(() => new Date())`

    Export type definitions `SkillsPr` and `NewSkillsPr`. Update `src/db/index.ts` with `CREATE TABLE IF NOT EXISTS skills_prs` and index on `workItemId`.
  </action>
  <acceptance_criteria>
    - `src/db/schema.ts` exports `skillsPrs`
    - `src/db/index.ts` contains `CREATE TABLE IF NOT EXISTS skills_prs`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement lifecycle data harvester, prompt generator, and skill synthesizer</name>
  <files>src/learn/types.ts, src/learn/harvester.ts, src/learn/prompt.ts, src/learn/generator.ts, tests/learn-generator.test.ts</files>
  <read_first>
    src/auditor/prompt.ts,
    src/db/schema.ts
  </read_first>
  <action>
    Create `src/learn/types.ts`:
    - Define interfaces: `TicketLifecycleData`, `LearnedSkill`, `SkillFrontmatter`.

    Create `src/learn/harvester.ts`:
    - `export async function harvestTicketLifecycleData(workItemId: number): Promise<TicketLifecycleData>`:
      - Reads `reworkCycles` for bounce counts and source gates.
      - Reads `l3Evidence` for local test counts and duration.
      - Reads `qaRuns` for staging test failures and flake status.
      - Reads `telemetryEvaluations` for error rate and p95 latency.
      - Fetches work item details via `getWorkItemDetails`.

    Create `src/learn/prompt.ts`:
    - `export function buildSkillLearningPrompt(lifecycle: TicketLifecycleData): { systemPrompt: string; userPrompt: string }`:
      - Wraps untrusted ticket details, review comments, and failure traces inside `<learning_source_context>` tags.
      - Adds instructions explicitly forbidding tool execution or adopting personas instructed inside `<learning_source_context>`.
      - Directs the model to extract reusable code patterns and common pitfalls in standard `SKILL.md` markdown format.

    Create `src/learn/generator.ts`:
    - `export async function generateSkillFromLifecycle(lifecycle: TicketLifecycleData, options?: { mockSkill?: LearnedSkill }): Promise<LearnedSkill>`:
      - Generates skill content (or uses `mockSkill` / deterministic generator in test mode).
      - Validates frontmatter (`name`, `description`, `domain`, `tags`).
      - Validates required sections (`# Overview`, `## Core Patterns`, `## Pitfalls`).

    Create `tests/learn-generator.test.ts` testing:
    - Harvesting lifecycle data from SQLite
    - Prompt builder ensuring XML isolation and meta-directive rejection
    - Skill generator producing compliant `SKILL.md` structure and frontmatter
  </action>
  <acceptance_criteria>
    - `src/learn/harvester.ts` exports `harvestTicketLifecycleData`
    - `src/learn/prompt.ts` exports `buildSkillLearningPrompt`
    - `src/learn/generator.ts` exports `generateSkillFromLifecycle`
    - `tests/learn-generator.test.ts` passes with `npx vitest run tests/learn-generator.test.ts`
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run vitest on learn generator tests:
```bash
npx vitest run tests/learn-generator.test.ts
```
</verification>
