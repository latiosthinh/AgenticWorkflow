---
phase: 02-execute-foundation-sandbox-dynamic-mcp-plan-checkpoint
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/db/schema.ts
  - src/db/index.ts
  - src/ado/work-item.ts
  - src/plan/schema.ts
  - src/plan/formatter.ts
  - src/plan/planner.ts
  - src/plan/checkpoint.ts
  - src/plan/watchdog.ts
  - src/execute/worker.ts
  - src/execute/router.ts
  - src/index.ts
  - tests/planner.test.ts
  - tests/plan-checkpoint.test.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02

must_haves:
  truths:
    - "Tickets with ambiguous requirements generate structured clarification questions posted to ADO discussion with [Plan Q&A] header"
    - "Plan questions tag work item with [awaiting-input] and immediately release git worktree resources"
    - "SQLite plan_checkpoints table persists pending questions with status='pending_human_input'"
    - "Non-bot discussion comment on [awaiting-input] ticket resumes run, incorporates answers, removes tag, and locks plan"
    - "Unanswered questions receive 24h reminder ping comment, and 72h unanswered transitions ticket to 'Blocked' state"
    - "Execute router dispatches 'New' tickets to auditor worker and 'In Dev' tickets or resumption comments to execute worker"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Drizzle schema extended with planCheckpoints table"
      contains: "planCheckpoints = sqliteTable('plan_checkpoints'"
    - path: "src/plan/schema.ts"
      provides: "Zod validation schema for plan reasoning and ambiguity outputs"
      exports: ["PlanResultSchema", "PlanResult"]
    - path: "src/plan/formatter.ts"
      provides: "HTML comment formatters with [Plan Q&A] and [Plan Checkpoint] headers"
      exports: ["formatPlanQuestionsComment", "formatPlanLockedComment"]
    - path: "src/plan/planner.ts"
      provides: "Planning agent evaluating ticket clarity with deterministic test fallback"
      exports: ["formulateImplementationPlan"]
    - path: "src/plan/checkpoint.ts"
      provides: "SQLite persistence repository for plan checkpoint state machine"
      exports: ["createPlanCheckpoint", "getPendingCheckpoint", "lockPlanCheckpoint", "updateCheckpointStatus"]
    - path: "src/plan/watchdog.ts"
      provides: "Background poller for 24h developer reminder pings and 72h blocker escalations"
      exports: ["checkPlanCheckpointTimeouts", "startPlanWatchdog"]
    - path: "src/execute/worker.ts"
      provides: "Execution orchestrator provisioning sandbox, resolving MCP, evaluating plan, and releasing worktree on Q&A"
      exports: ["processWorkItemExecute"]
    - path: "src/execute/router.ts"
      provides: "Unified webhook dispatcher routing 'New' tickets to auditor and 'In Dev' tickets to execute worker"
      exports: ["routeWorkItemEvent"]
    - path: "tests/planner.test.ts"
      provides: "Unit tests for plan generation, ambiguity detection, and ADO comment formatting"
    - path: "tests/plan-checkpoint.test.ts"
      provides: "Integration tests for checkpoint persistence, worktree release, webhook comment resumption, and watchdog"
  key_links:
    - from: "src/execute/worker.ts"
      to: "src/sandbox/worktree.ts"
      via: "createWorktree and cleanupWorktree invocations"
      pattern: "createWorktree|cleanupWorktree"
    - from: "src/execute/worker.ts"
      to: "src/mcp/registry.ts"
      via: "createDynamicMcpTools invocation"
      pattern: "createDynamicMcpTools"
    - from: "src/execute/worker.ts"
      to: "src/plan/planner.ts"
      via: "formulateImplementationPlan invocation"
      pattern: "formulateImplementationPlan"
    - from: "src/execute/worker.ts"
      to: "src/plan/checkpoint.ts"
      via: "createPlanCheckpoint and lockPlanCheckpoint state updates"
      pattern: "createPlanCheckpoint|lockPlanCheckpoint"
    - from: "src/index.ts"
      to: "src/execute/router.ts"
      via: "registerWorkItemHandler(routeWorkItemEvent)"
      pattern: "registerWorkItemHandler\\(routeWorkItemEvent\\)"
---

<objective>
Implement the interactive plan checkpoint (`Q→human`) lifecycle, webhook resumption, watchdog reminders, and background execution orchestration.

Purpose: Formulate concrete implementation plans, post interactive clarification questions to Azure DevOps discussion when ambiguities exist, release host sandbox resources immediately while waiting for human developers, re-trigger execution upon comment replies, and lock verified plans prior to Phase 3 code edits.
Output: Drizzle plan checkpoint table, planning agent, ADO HTML comment formatters, 24h/72h watchdog poller, execution worker, and unified webhook router.
</objective>

<execution_context>
@$HOME/.config/antigravity/get-shit-done/workflows/execute-plan.md
@$HOME/.config/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-execute-foundation-sandbox-dynamic-mcp-plan-checkpoint/02-CONTEXT.md
@.planning/phases/02-execute-foundation-sandbox-dynamic-mcp-plan-checkpoint/02-RESEARCH.md
@.planning/phases/02-execute-foundation-sandbox-dynamic-mcp-plan-checkpoint/02-PATTERNS.md
@.planning/phases/02-execute-foundation-sandbox-dynamic-mcp-plan-checkpoint/02-VALIDATION.md
@.planning/phases/02-execute-foundation-sandbox-dynamic-mcp-plan-checkpoint/02-01-SUMMARY.md
@.planning/phases/02-execute-foundation-sandbox-dynamic-mcp-plan-checkpoint/02-02-SUMMARY.md
</context>

<interfaces>
<!-- Contracts from Plans 01 and 02 used by execution worker -->
From src/sandbox/worktree.ts:
```typescript
export interface WorktreeResult {
  worktreePath: string;
  branchName: string;
  testFilesProtected: string[];
}
export function createWorktree(
  repoRoot: string,
  workItemId: number,
  title: string,
  baseBranch?: string
): Promise<WorktreeResult>;
export function cleanupWorktree(
  repoRoot: string,
  worktreePath: string,
  options?: { deleteBranch?: boolean; branchName?: string }
): Promise<void>;
```

From src/mcp/registry.ts:
```typescript
export interface ToolRegistryOptions {
  worktreePath: string;
  tags: string[];
  knownSecrets?: string[];
}
export interface DynamicMcpSession {
  tools: Record<string, any>;
  activeTools: string[];
  activeToolCount: number;
  close: () => Promise<void>;
}
export function createDynamicMcpTools(options: ToolRegistryOptions): Promise<DynamicMcpSession>;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Drizzle Plan Schema, Planning Agent with Ambiguity Evaluator & ADO HTML Formatter (PLAN-01)</name>
  <read_first>
    - src/db/schema.ts
    - src/db/index.ts
    - src/auditor/evaluator.ts
    - src/ado/formatter.ts
    - .planning/phases/02-execute-foundation-sandbox-dynamic-mcp-plan-checkpoint/02-RESEARCH.md
    - .planning/phases/02-execute-foundation-sandbox-dynamic-mcp-plan-checkpoint/02-PATTERNS.md
  </read_first>
  <files>
    src/db/schema.ts,
    src/db/index.ts,
    src/plan/schema.ts,
    src/plan/formatter.ts,
    src/plan/planner.ts,
    tests/planner.test.ts
  </files>
  <action>
    Implement Drizzle database schema, Zod plan schema, planning agent, and ADO formatters per CONTEXT.md and RESEARCH.md:
    1. Update `src/db/schema.ts`:
       - Define and export `planCheckpoints = sqliteTable('plan_checkpoints', { ... })`:
         - `id`: `integer('id').primaryKey({ autoIncrement: true })`
         - `workItemId`: `integer('work_item_id').notNull()`
         - `revId`: `integer('rev_id').notNull()`
         - `status`: `text('status', { enum: ['pending_human_input', 'resumed', 'locked', 'blocked', 'expired'] }).notNull().default('pending_human_input')`
         - `questions`: `text('questions').notNull()` (JSON string array)
         - `answers`: `text('answers')`
         - `planMarkdown`: `text('plan_markdown')`
         - `estimatedFiles`: `text('estimated_files')` (JSON string array)
         - `testStrategy`: `text('test_strategy')`
         - `remindedAt`: `integer('reminded_at', { mode: 'timestamp' })`
         - `escalatedAt`: `integer('escalated_at', { mode: 'timestamp' })`
         - `createdAt`: `integer('created_at', { mode: 'timestamp' }).notNull().$defaultFn(() => new Date())`
         - `updatedAt`: `integer('updated_at', { mode: 'timestamp' }).notNull().$defaultFn(() => new Date())`
       - Export types `PlanCheckpoint` and `InsertPlanCheckpoint`.
    2. Update `src/db/index.ts`:
       - Add raw DDL execution for `plan_checkpoints` inside `sqlite.exec`:
         `CREATE TABLE IF NOT EXISTS plan_checkpoints ( id INTEGER PRIMARY KEY AUTOINCREMENT, work_item_id INTEGER NOT NULL, rev_id INTEGER NOT NULL, status TEXT NOT NULL DEFAULT 'pending_human_input', questions TEXT NOT NULL, answers TEXT, plan_markdown TEXT, estimated_files TEXT, test_strategy TEXT, reminded_at INTEGER, escalated_at INTEGER, created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL );`
    3. Create `src/plan/schema.ts`:
       - Define `PlanResultSchema = z.object({ hasAmbiguities: z.boolean().describe('True if clarification from human developer is required before coding'), questions: z.array(z.string()).describe('Structured questions addressing missing technical decisions or ambiguities'), planMarkdown: z.string().describe('Concrete step-by-step implementation plan'), estimatedFiles: z.array(z.string()).describe('List of files to be created or modified'), testStrategy: z.string().describe('Unit and integration testing plan to verify changes') })`.
       - Export `PlanResult = z.infer<typeof PlanResultSchema>`.
    4. Create `src/plan/formatter.ts`:
       - `formatPlanQuestionsComment(questions: string[]): string`:
         - Render markdown with `### [Plan Q&A] Implementation Clarification Required`, numbered questions, action instructions (`Reply directly to this discussion with your clarifications. Work item is tagged [awaiting-input]. Sandbox execution resources have been released while awaiting your reply.`), and suffix `<!-- [automated-agent] -->`.
         - Sanitize HTML with `sanitize-html` allowing headers, lists, code, and strong tags.
       - `formatPlanLockedComment(planMarkdown: string, estimatedFiles: string[]): string`:
         - Render markdown with `### [Plan Checkpoint] Implementation Plan Locked`, estimated files bullet list, planMarkdown body, and suffix `<!-- [automated-agent] -->`.
         - Sanitize HTML and return string.
    5. Create `src/plan/planner.ts`:
       - `formulateImplementationPlan(ticket: { title: string; description: string; acceptanceCriteria: string; tags?: string[] }, opts?: { forceAi?: boolean }): Promise<PlanResult>`:
         - Deterministic test fallback when `env.NODE_ENV === 'test' && !opts?.forceAi`:
           - If `ticket.title.includes('[ambiguous]') || ticket.description.includes('TBD') || !ticket.acceptanceCriteria`:
             Return `{ hasAmbiguities: true, questions: ['Which database schema should be modified?', 'What is the expected error status code?'], planMarkdown: '', estimatedFiles: [], testStrategy: '' }`.
           - Else:
             Return `{ hasAmbiguities: false, questions: [], planMarkdown: '### Implementation Steps\n1. Modify service layer\n2. Add unit tests', estimatedFiles: ['src/service.ts', 'tests/service.test.ts'], testStrategy: 'Run vitest unit suite' }`.
         - Live AI generation when `env.NODE_ENV !== 'test' || opts?.forceAi`:
           - Use `generateText` with `openai('gpt-4o')` and `output: Output.object({ schema: PlanResultSchema })`.
         - Add ponytail comment: `// ponytail: deterministic offline planner fallback in test env; enable live model in staging`
    6. Create `tests/planner.test.ts`:
       - Test `PlanResultSchema` validation on valid and invalid payloads.
       - Test `formulateImplementationPlan` deterministic fallback with clear vs ambiguous tickets.
       - Test `formatPlanQuestionsComment` outputs HTML containing `[Plan Q&A]` and `<!-- [automated-agent] -->`.
       - Test `formatPlanLockedComment` outputs HTML containing `[Plan Checkpoint]` and `<!-- [automated-agent] -->`.
  </action>
  <acceptance_criteria>
    - File `src/db/schema.ts` defines `planCheckpoints` table
    - File `src/db/index.ts` creates `plan_checkpoints` table via raw DDL
    - File `src/plan/schema.ts` exports `PlanResultSchema`
    - File `src/plan/formatter.ts` exports `formatPlanQuestionsComment` and `formatPlanLockedComment`
    - File `src/plan/planner.ts` exports `formulateImplementationPlan`
    - `tests/planner.test.ts` passes via `npx vitest run tests/planner.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/planner.test.ts</automated>
  </verify>
  <done>Plan checkpoint schema, Zod validation, planning agent with ambiguity evaluator, and ADO HTML formatters implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Checkpoint Persistence, ADO Tag Patching & 24h/72h Watchdog (PLAN-02)</name>
  <read_first>
    - src/ado/work-item.ts
    - src/ado/client.ts
    - src/db/schema.ts
    - .planning/phases/02-execute-foundation-sandbox-dynamic-mcp-plan-checkpoint/02-RESEARCH.md
    - .planning/phases/02-execute-foundation-sandbox-dynamic-mcp-plan-checkpoint/02-PATTERNS.md
  </read_first>
  <files>
    src/ado/work-item.ts,
    src/plan/checkpoint.ts,
    src/plan/watchdog.ts
  </files>
  <action>
    Extend ADO work item patching with tag management and implement checkpoint persistence repository and timeout watchdog:
    1. Update `src/ado/work-item.ts`:
       - Update `WorkItemDetails` interface to add `tags?: string; history?: string`.
       - In `getWorkItemDetails`: extract `tags: fields['System.Tags'] || ''` and `history: fields['System.History'] || ''`.
       - Export helper `buildTagPatch(currentTags: string | undefined, tagToAdd?: string, tagToRemove?: string): JsonPatchDocument`:
         - Parse existing tags splitting by `;` and trimming whitespace.
         - If `tagToAdd` provided and not present, add it.
         - If `tagToRemove` provided and present, filter it out.
         - If updated list is empty, remove `/fields/System.Tags` or set to empty string.
         - Return JSON Patch with `op: Operation.Replace` (or `Operation.Add` if `currentTags` was undefined).
       - Export helper `buildPlanQuestionPatch(htmlComment: string, currentTags?: string): JsonPatchDocument`:
         - Combines tag update adding `[awaiting-input]` with history comment update.
       - Export helper `buildPlanLockedPatch(htmlComment: string, currentTags?: string): JsonPatchDocument`:
         - Combines tag update removing `[awaiting-input]` with history comment update.
       - Export `updateWorkItemTags(workItemId: number, tagToAdd?: string, tagToRemove?: string): Promise<any>`.
    2. Create `src/plan/checkpoint.ts`:
       - `createPlanCheckpoint(data: { workItemId: number; revId: number; questions: string[]; planMarkdown?: string; estimatedFiles?: string[]; testStrategy?: string }): Promise<PlanCheckpoint>`:
         - Inserts row into `planCheckpoints` table with `status: 'pending_human_input'`, `questions: JSON.stringify(data.questions)`, `planMarkdown: data.planMarkdown || null`, `estimatedFiles: data.estimatedFiles ? JSON.stringify(data.estimatedFiles) : null`, `testStrategy: data.testStrategy || null`.
       - `getPendingCheckpoint(workItemId: number): Promise<PlanCheckpoint | undefined>`:
         - Queries `planCheckpoints` where `workItemId = ?` and `status = 'pending_human_input'`, ordered by `createdAt desc` limit 1.
       - `lockPlanCheckpoint(id: number, answers: string, updatedPlan?: string): Promise<void>`:
         - Updates row setting `status: 'locked'`, `answers`, optional `planMarkdown`, `updatedAt: new Date()`.
       - `updateCheckpointStatus(id: number, status: 'pending_human_input' | 'resumed' | 'locked' | 'blocked' | 'expired'): Promise<void>`.
    3. Create `src/plan/watchdog.ts`:
       - Constants: `TWENTY_FOUR_HOURS_MS = 24 * 60 * 60 * 1000`, `SEVENTY_TWO_HOURS_MS = 72 * 60 * 60 * 1000`.
       - `checkPlanCheckpointTimeouts(): Promise<{ reminded: number; escalated: number }>`:
         - Select all rows from `planCheckpoints` where `status = 'pending_human_input'`.
         - For each checkpoint:
           - Calculate `elapsed = Date.now() - cp.createdAt.getTime()`.
           - 72h Escalation (`elapsed >= SEVENTY_TWO_HOURS_MS && !cp.escalatedAt`):
             - Post discussion comment: `<strong>[Plan Checkpoint] Escalation: Work Item Blocked</strong><p>Clarification questions have been unanswered for over 72 hours. Marking work item Blocked.</p><!-- [automated-agent] -->`.
             - Update ADO work item state to `'Blocked'`: `adoClient.updateWorkItem(cp.workItemId, [{ op: Operation.Replace, path: '/fields/System.State', value: 'Blocked' }])`.
             - Update DB checkpoint: `status: 'blocked'`, `escalatedAt: new Date()`, `updatedAt: new Date()`.
             - Increment `escalated`.
           - 24h Reminder (`elapsed >= TWENTY_FOUR_HOURS_MS && !cp.remindedAt`):
             - Post discussion comment: `<strong>[Plan Reminder] Action Required: Unanswered Questions</strong><p>The implementation plan is awaiting developer reply. Please respond to the questions above to resume work.</p><!-- [automated-agent] -->`.
             - Update DB checkpoint: `remindedAt: new Date()`, `updatedAt: new Date()`.
             - Increment `reminded`.
       - `startPlanWatchdog(intervalMs = 60 * 60 * 1000): { stop: () => void }`:
         - Runs `checkPlanCheckpointTimeouts()` on interval, with exception handling.
         - Returns stop function clearing interval.
  </action>
  <acceptance_criteria>
    - `src/ado/work-item.ts` exports `buildTagPatch`, `buildPlanQuestionPatch`, `buildPlanLockedPatch`
    - `src/ado/work-item.ts` extracts `tags` and `history` in `getWorkItemDetails`
    - `src/plan/checkpoint.ts` exports `createPlanCheckpoint`, `getPendingCheckpoint`, `lockPlanCheckpoint`
    - `src/plan/watchdog.ts` exports `checkPlanCheckpointTimeouts` and `startPlanWatchdog`
    - Watchdog triggers 24h reminder comment and 72h Blocked escalation state transition
  </acceptance_criteria>
  <verify>
    <automated>npx tsx -e "import { checkPlanCheckpointTimeouts } from './src/plan/watchdog.js'; import { createPlanCheckpoint } from './src/plan/checkpoint.js'; console.log('Checkpoint repo and watchdog compiled successfully');"</automated>
  </verify>
  <done>Plan checkpoint persistence, ADO tag patch helpers, and 24h/72h timeout watchdog implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Worker Orchestration, Webhook Router & Sandbox Release/Resumption Integration (PLAN-01, PLAN-02)</name>
  <read_first>
    - src/auditor/worker.ts
    - src/ingress/routes.ts
    - src/sandbox/worktree.ts
    - src/mcp/registry.ts
    - src/plan/checkpoint.ts
    - src/plan/planner.ts
    - src/index.ts
    - .planning/phases/02-execute-foundation-sandbox-dynamic-mcp-plan-checkpoint/02-RESEARCH.md
    - .planning/phases/02-execute-foundation-sandbox-dynamic-mcp-plan-checkpoint/02-PATTERNS.md
  </read_first>
  <files>
    src/execute/worker.ts,
    src/execute/router.ts,
    src/index.ts,
    tests/plan-checkpoint.test.ts
  </files>
  <action>
    Implement execution orchestrator, unified webhook router, and integration tests per CONTEXT.md and RESEARCH.md:
    1. Create `src/execute/worker.ts`:
       - `processWorkItemExecute(workItemId: number, revId: number): Promise<void>`:
         - Step 1: Fetch `workItem = await getWorkItemDetails(workItemId)`.
         - Step 2: Check for Resumption Flow:
           - Check if ticket tags include `[awaiting-input]`.
           - Look up `pendingCheckpoint = await getPendingCheckpoint(workItemId)`.
           - If `pendingCheckpoint` exists and non-bot reply is present in `workItem.history`:
             - Incorporate answers from `workItem.history`.
             - Lock plan checkpoint: `await lockPlanCheckpoint(pendingCheckpoint.id, workItem.history)`.
             - Format locked plan comment: `formatPlanLockedComment(pendingCheckpoint.planMarkdown || 'Implementation plan locked with developer clarifications.', JSON.parse(pendingCheckpoint.estimatedFiles || '[]'))`.
             - Patch ADO work item: remove `[awaiting-input]` tag and post locked comment via `buildPlanLockedPatch`.
             - Update `dedupEvents` status to `'completed'`.
             - Log: `Plan locked for ticket ${workItemId} with developer answers. Ready for Phase 3 implementation.`
             - Return.
         - Step 3: Fresh 'In Dev' Execution Flow:
           - If `workItem.state !== 'In Dev'`, update `dedupEvents` status to `'skipped'` and return.
           - Provision ephemeral git worktree:
             `const { worktreePath, branchName } = await createWorktree(process.cwd(), workItemId, workItem.title);`
           - Resolve dynamic MCP tools:
             `const tags = (workItem.tags || '').split(';').map(t => t.trim()).filter(Boolean);`
             `const mcpSession = await createDynamicMcpTools({ worktreePath, tags });`
           - Formulate implementation plan:
             `const plan = await formulateImplementationPlan({ title: workItem.title, description: workItem.description, acceptanceCriteria: workItem.acceptanceCriteria, tags });`
           - Handle Plan Ambiguities:
             - If `plan.hasAmbiguities`:
               - Insert checkpoint: `await createPlanCheckpoint({ workItemId, revId, questions: plan.questions, planMarkdown: plan.planMarkdown, estimatedFiles: plan.estimatedFiles, testStrategy: plan.testStrategy });`
               - Format comment: `const comment = formatPlanQuestionsComment(plan.questions);`
               - Patch ADO work item: add `[awaiting-input]` tag and post comment via `buildPlanQuestionPatch`.
               - IMMEDIATELY release worktree: `await cleanupWorktree(process.cwd(), worktreePath);` (SANDBOX RELEASED per locked decision)
               - Close MCP session: `await mcpSession.close();`
               - Update `dedupEvents` status to `'completed'`.
               - Return.
             - If NO ambiguities:
               - Insert locked checkpoint directly: `await createPlanCheckpoint(...)` and set `status = 'locked'`.
               - Format locked comment: `const comment = formatPlanLockedComment(plan.planMarkdown, plan.estimatedFiles);`
               - Post locked comment to ADO discussion.
               - Close MCP session: `await mcpSession.close();`
               - Update `dedupEvents` status to `'completed'`.
               - Return.
         - Add error handling updating `dedupEvents` to `'failed'` on unhandled rejection.
    2. Create `src/execute/router.ts`:
       - `routeWorkItemEvent(workItemId: number, revId: number): Promise<void>`:
         - Fetch `workItem = await getWorkItemDetails(workItemId);`
         - If `workItem.state === 'New'`, invoke `processWorkItemAudit(workItemId, revId);`
         - Else if `workItem.state === 'In Dev' || (workItem.tags && workItem.tags.includes('[awaiting-input]'))`, invoke `processWorkItemExecute(workItemId, revId);`
         - Else:
           - Update `dedupEvents` to `'skipped'` (`Ticket state '${workItem.state}' has no active handler`).
    3. Update `src/index.ts`:
       - Import `routeWorkItemEvent` from `./execute/router.js`.
       - Import `startPlanWatchdog` from `./plan/watchdog.js`.
       - In `buildApp()`: replace `registerWorkItemHandler(processWorkItemAudit)` with `registerWorkItemHandler(routeWorkItemEvent)`.
       - In `startServer()`: start watchdog `const watchdog = startPlanWatchdog()`, add `watchdog.stop()` to `gracefulShutdown()`.
    4. Create `tests/plan-checkpoint.test.ts`:
       - Test `createPlanCheckpoint`, `getPendingCheckpoint`, `lockPlanCheckpoint` CRUD in SQLite.
       - Test watchdog:
         - Insert pending checkpoint created 25h ago -> runs `checkPlanCheckpointTimeouts()` -> asserts reminder comment posted and `remindedAt` set.
         - Insert pending checkpoint created 75h ago -> runs `checkPlanCheckpointTimeouts()` -> asserts state updated to `Blocked` and checkpoint status updated to `blocked`.
       - Test `routeWorkItemEvent`:
         - Ticket in `New` state dispatches to auditor.
         - Ticket in `In Dev` state dispatches to execute worker.
       - Test execution worker ambiguity flow:
         - Ambiguous ticket creates worktree, posts `[Plan Q&A]` comment, tags `[awaiting-input]`, and invokes `cleanupWorktree` (verifies worktree directory does not exist on disk).
       - Test execution worker resumption flow:
         - Ticket with `[awaiting-input]` receiving comment updates checkpoint to `locked`, removes `[awaiting-input]` tag, and posts `[Plan Checkpoint]` locked comment.
  </action>
  <acceptance_criteria>
    - File `src/execute/worker.ts` exports `processWorkItemExecute`
    - `src/execute/worker.ts` releases worktree via `cleanupWorktree` immediately when ambiguities are detected
    - `src/execute/worker.ts` locks plan and removes `[awaiting-input]` tag when non-bot comment received
    - File `src/execute/router.ts` exports `routeWorkItemEvent` routing `New` to auditor and `In Dev` to execute worker
    - File `src/index.ts` registers `routeWorkItemEvent` as the active work item handler
    - `tests/plan-checkpoint.test.ts` passes via `npx vitest run tests/plan-checkpoint.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/plan-checkpoint.test.ts</automated>
  </verify>
  <done>Plan checkpoint lifecycle, worktree release on clarification, comment resumption, watchdog poller, and unified execution routing fully integrated and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer Comment -> Checkpoint Resumption | Human reply in ADO discussion thread crossing into automated execution pipeline. |
| Ingress Webhook -> Execute Router | Validated ADO webhook payloads dispatched to audit worker vs execute worker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-07 | Denial of Service | `src/execute/worker.ts` | mitigate | Immediately release git worktree and exit worker process when questions are posted so host resources are not locked. |
| T-2-08 | Denial of Service | `src/execute/worker.ts` | mitigate | Append `<!-- [automated-agent] -->` to all posted comments to prevent recursive webhook loops via `isBotEcho`. |
| T-2-09 | Tampering | `src/execute/worker.ts` | mitigate | Only non-bot comments without `[automated-agent]` marker can resume and lock plan checkpoints. |
</threat_model>

<verification>
Run planner and plan checkpoint test suites:
- `npx vitest run tests/planner.test.ts`
- `npx vitest run tests/plan-checkpoint.test.ts`
- Full test suite: `npm test`
</verification>

<success_criteria>
Ambiguous tickets post structured Q&A comments and immediately release worktrees; developer replies resume and lock plans; 24h ping reminders and 72h blocker escalations trigger as specified; execute router unifies Phase 1 and Phase 2 workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/02-execute-foundation-sandbox-dynamic-mcp-plan-checkpoint/02-03-SUMMARY.md`
</output>
