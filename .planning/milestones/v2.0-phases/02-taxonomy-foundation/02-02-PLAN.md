---
phase: 02-taxonomy-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/execute/router.ts
  - tests/lifecycle-replay.test.ts
autonomous: true
requirements:
  - TAX-03

must_haves:
  truths:
    - "src/execute/router.ts resolves incoming work items using resolveRoutingStep from src/pipeline/taxonomy.ts"
    - "Human acceptance verdicts (reset_rework, approve, reject) evaluate with precedence before state routing"
    - "Worker dispatch preserves options forwarding and exact dedup skip message strings"
    - "Lifecycle replay test drives fixture revisions New -> Ready to Dev -> In Dev -> Dev Done -> Ready for QA -> Ready to Deploy -> Done asserting identical handler dispatch to v1.0"
  artifacts:
    - path: "src/execute/router.ts"
      provides: "Refactored router dispatching via taxonomy resolution"
      exports: ["routeWorkItemEvent"]
    - path: "tests/lifecycle-replay.test.ts"
      provides: "Full lifecycle replay parity integration test"
      min_lines: 100
  key_links:
    - from: "src/execute/router.ts"
      to: "src/pipeline/taxonomy.ts"
      via: "resolveRoutingStep(workItem.state, workItem.tags)"
      pattern: "resolveRoutingStep\\(workItem\\.state"
    - from: "tests/lifecycle-replay.test.ts"
      to: "src/execute/router.ts"
      via: "routeWorkItemEvent calls across all lifecycle states"
      pattern: "routeWorkItemEvent\\(workItemId"
---

<objective>
Refactor `src/execute/router.ts` to dispatch via the taxonomy resolution helper `resolveRoutingStep` with zero behavior change to shipped v1.0 paths, and prove lifecycle parity through a comprehensive replay integration test `tests/lifecycle-replay.test.ts`.

Purpose: Bind the runtime router to the canonical Golden Path v2 taxonomy while preserving exact options forwarding, acceptance verdict evaluation precedence, and dedup skip strings.
Output: Refactored `src/execute/router.ts` and new `tests/lifecycle-replay.test.ts`.
</objective>

<execution_context>
@$HOME/.config/antigravity/get-shit-done/workflows/execute-plan.md
@$HOME/.config/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-taxonomy-foundation/02-RESEARCH.md
@.planning/phases/02-taxonomy-foundation/02-PATTERNS.md
@.planning/phases/02-taxonomy-foundation/02-01-PLAN.md
@src/pipeline/taxonomy.ts
@src/execute/router.ts

<interfaces>
<!-- Taxonomy helper imported into router -->
import { resolveRoutingStep } from '../pipeline/taxonomy.js';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor router to dispatch state transitions via taxonomy resolution</name>
  <files>src/execute/router.ts</files>
  <read_first>
    - src/execute/router.ts
    - src/pipeline/taxonomy.ts
    - .planning/phases/02-taxonomy-foundation/02-RESEARCH.md
    - .planning/phases/02-taxonomy-foundation/02-PATTERNS.md
  </read_first>
  <behavior>
    - Preserve existing acceptance verdict detection (reset_rework, approve, reject) and evaluate it BEFORE state transition routing.
    - Resolve taxonomy step via `const step = resolveRoutingStep(workItem.state, workItem.tags)`.
    - If no step is resolved, record dedup status 'skipped' with exact template literal `Ticket state '${workItem.state}' has no active handler` and return.
    - Switch on `step.step`:
      * case 1: call `processWorkItemAudit(workItemId, revId)`
      * case 3: call `processWorkItemExecute(workItemId, revId, options)`
      * case 4: execute existing Dev Done pull request logic (extract latest l3Evidence, format description, call `createOrGetPullRequest`, mark 'completed')
      * case 6: call `processQaVerification(workItemId, options)` and mark 'completed'
      * case 7: call `processDeploymentWorkflow(workItemId, revId, options)` and mark 'completed'
      * default: record dedup status 'skipped' with exact template literal `Ticket state '${workItem.state}' has no active handler`
    - Preserve options forwarding to all worker calls (options passed to execute, QA, deploy, and rework).
    - Retain existing error handling catch block marking dedup 'failed' and logging error.
  </behavior>
  <action>
    Refactor `src/execute/router.ts` in-place using `resolveRoutingStep` and the step-based switch per 02-PATTERNS.md lines 130-274, maintaining exact handler signatures and dedup status messages.
  </action>
  <verify>
    <automated>npx vitest run tests/worker.test.ts tests/rework-integration.test.ts tests/deploy-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/execute/router.ts` dispatches handlers based on `step.step`.
    - Acceptance verdicts continue taking precedence over state-based routing.
    - Existing worker, rework, and deploy test suites pass without regressions.
  </acceptance_criteria>
  <done>Router refactored to delegate through taxonomy resolution with all existing tests passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build complete v1 lifecycle replay parity test</name>
  <files>tests/lifecycle-replay.test.ts</files>
  <read_first>
    - src/execute/router.ts
    - src/pipeline/taxonomy.ts
    - tests/rework-integration.test.ts
    - tests/worker.test.ts
    - .planning/phases/02-taxonomy-foundation/02-PATTERNS.md
  </read_first>
  <behavior>
    - Initialize isolated StateStore test directory via `createTestStateStore()`.
    - Mock ADO WorkItemTrackingApi on `adoClient`.
    - Drive a single fixture work item through all 7 primary lifecycle states and verify handler behavior:
      1. Revision 1: State 'New' -> calls audit worker -> marks dedup completed.
      2. Revision 2: State 'Ready to Dev' -> unhandled -> marks dedup skipped with `Ticket state 'Ready to Dev' has no active handler`.
      3. Revision 3: State 'In Dev' -> calls execution worker -> options forwarded.
      4. Revision 4: State 'Dev Done' -> calls PR creation -> marks dedup completed.
      5. Revision 5: State 'Dev Done' with history '[approve-acceptance]' -> acceptance verdict takes precedence -> tags updated -> marks dedup completed.
      6. Revision 6: State 'Ready for QA' -> calls QA worker -> marks dedup completed.
      7. Revision 7: State 'Ready to Deploy' -> calls deploy worker -> marks dedup completed.
      8. Revision 8: State 'Done' -> unhandled -> marks dedup skipped with `Ticket state 'Done' has no active handler`.
    - Run full test suite `npm test` ensuring all 312+ tests pass.
  </behavior>
  <action>
    Create `tests/lifecycle-replay.test.ts` following pattern in 02-PATTERNS.md lines 376-460, implementing the end-to-end replay sequence across all lifecycle states.
  </action>
  <verify>
    <automated>npx vitest run tests/lifecycle-replay.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `tests/lifecycle-replay.test.ts` passes with all lifecycle transitions verified.
    - Entire repository test suite (`npm test`) passes with zero failures.
  </acceptance_criteria>
  <done>Full lifecycle replay test implemented and verified against entire test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADO Webhook Ingress -> Router | Untrusted state strings or manipulated work item tags in webhook payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | `src/execute/router.ts` | mitigate | Exact matching of ADO states via `resolveRoutingStep` with safe fallback to `skipped` dedup status on unknown states, preventing arbitrary or corrupted state transitions |
| T-02-03 | Denial of Service | `src/execute/router.ts` | mitigate | Router wraps dispatch in try/catch block, recording `failed` dedup status before rethrowing to ensure queue lane error accounting |
</threat_model>

<verification>
Automated test suite execution:
`npx vitest run tests/lifecycle-replay.test.ts && npm test`
</verification>

<success_criteria>
1. `src/execute/router.ts` delegates transition dispatch to `resolveRoutingStep` from `src/pipeline/taxonomy.ts`.
2. Options forwarding and exact dedup skip strings are strictly preserved.
3. `tests/lifecycle-replay.test.ts` exercises all 7 ADO lifecycle states and passes.
4. Full test suite (`npm test`) remains 100% green with zero regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-taxonomy-foundation/02-02-SUMMARY.md`
</output>
