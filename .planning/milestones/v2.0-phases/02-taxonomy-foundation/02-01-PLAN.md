---
phase: 02-taxonomy-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/taxonomy.ts
  - tests/taxonomy.test.ts
autonomous: true
requirements:
  - TAX-01
  - TAX-03

must_haves:
  truths:
    - "GOLDEN_PATH_V2 in src/pipeline/taxonomy.ts models all 9 steps across 5 columns with actors, evidence levels, and ADO states"
    - "GOLDEN_PATH_V2 data structure and its child step definitions are immutable via Object.freeze"
    - "src/pipeline/taxonomy.ts has zero runtime dependencies and imports no external modules or files"
    - "Taxonomy lookup helpers resolve steps by number, column, ADO state, and routing context"
  artifacts:
    - path: "src/pipeline/taxonomy.ts"
      provides: "Golden Path v2 canonical taxonomy definition and query helpers"
      exports: ["GOLDEN_PATH_V2", "resolveRoutingStep", "getStepByNumber", "getStepsByColumn", "getStepsByAdoState"]
    - path: "tests/taxonomy.test.ts"
      provides: "Unit test suite for taxonomy mapping, immutability, and resolution helpers"
      min_lines: 60
  key_links:
    - from: "tests/taxonomy.test.ts"
      to: "src/pipeline/taxonomy.ts"
      via: "import of GOLDEN_PATH_V2 and lookup functions"
      pattern: "import.*GOLDEN_PATH_V2.*from '\\.\\./src/pipeline/taxonomy\\.js'"
---

<objective>
Establish the canonical Golden Path v2 taxonomy foundation in `src/pipeline/taxonomy.ts` as a frozen, type-safe data model mapping all 9 steps across 5 columns, actor roles (⚡ AI / 👤 Human), evidence levels (L1–L7), and ADO wire states with zero runtime dependencies.

Purpose: Provide a single authoritative data source for pipeline stage classification, routing resolution, and evidence indexing without renaming directories or modifying ADO wire contracts.
Output: `src/pipeline/taxonomy.ts` and `tests/taxonomy.test.ts`.
</objective>

<execution_context>
@$HOME/.config/antigravity/get-shit-done/workflows/execute-plan.md
@$HOME/.config/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-taxonomy-foundation/02-CONTEXT.md
@.planning/phases/02-taxonomy-foundation/02-RESEARCH.md
@.planning/phases/02-taxonomy-foundation/02-PATTERNS.md
@src/accept/verdict.ts

<interfaces>
<!-- Types and structure to establish in src/pipeline/taxonomy.ts -->
export type ColumnId =
  | 'REFINEMENT'
  | 'EXECUTION'
  | 'ACCEPTANCE'
  | 'RELEASE'
  | 'RETRO';

export type StepNumber = 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | 9;
export type ActorRole = 'AI' | 'Human';
export type EvidenceLevel = 'L1' | 'L2' | 'L3' | 'L4' | 'L5' | 'L6' | 'L7';
export type AdoState =
  | 'New'
  | 'Ready to Dev'
  | 'In Dev'
  | 'Dev Done'
  | 'Ready for QA'
  | 'Ready to Deploy'
  | 'Done';

export interface StepDefinition {
  readonly step: StepNumber;
  readonly name: string;
  readonly column: ColumnId;
  readonly actor: ActorRole;
  readonly actorDetail: string;
  readonly evidenceLevels: readonly EvidenceLevel[];
  readonly primaryEvidenceLevel: EvidenceLevel;
  readonly adoState: AdoState;
  readonly gateType: 'human_verdict' | 'automated_trigger';
  readonly keyTags?: readonly string[];
  readonly description?: string;
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Golden Path v2 taxonomy types, constants, and lookup helpers</name>
  <files>src/pipeline/taxonomy.ts</files>
  <read_first>
    - .planning/phases/02-taxonomy-foundation/02-RESEARCH.md
    - .planning/phases/02-taxonomy-foundation/02-PATTERNS.md
    - src/accept/verdict.ts
  </read_first>
  <behavior>
    - Ensure pure TypeScript module with zero external or internal imports.
    - Export union types ColumnId, StepNumber, ActorRole, EvidenceLevel, AdoState, and StepDefinition interface.
    - Export GOLDEN_PATH_V2 array deeply frozen with Object.freeze containing all 9 steps per Authoritative State Matrix:
      * Step 1: 'Ticket & AC verify', REFINEMENT, AI ('AI Agent'), evidence: ['L1'], primary: 'L1', state: 'New', gate: 'automated_trigger', tags: ['[audit-passed]', '[awaiting-scope-lock]']
      * Step 2: 'Scope review & verify', REFINEMENT, Human ('Human PM'), evidence: ['L1'], primary: 'L1', state: 'Ready to Dev', gate: 'human_verdict', tags: ['[awaiting-scope-lock]', '[scope-locked]']
      * Step 3: 'Loop: Plan-Code-Test', EXECUTION, AI ('AI Agent'), evidence: ['L2', 'L3'], primary: 'L3', state: 'In Dev', gate: 'automated_trigger', tags: ['[awaiting-input]']
      * Step 4: 'Dev validate & PR', EXECUTION, Human ('Human Dev'), evidence: ['L2', 'L3'], primary: 'L2', state: 'Dev Done', gate: 'human_verdict', tags: ['[awaiting-acceptance]', '[acceptance-approved]']
      * Step 5: 'PR review & CI deploy', ACCEPTANCE, Human ('Human TechLead/SA'), evidence: ['L3', 'L4'], primary: 'L4', state: 'Dev Done', gate: 'human_verdict', tags: ['[pr-merged]']
      * Step 6: 'QA staging verify', ACCEPTANCE, Human ('Human QA'), evidence: ['L3', 'L5'], primary: 'L5', state: 'Ready for QA', gate: 'human_verdict', tags: ['[qa-verified]', '[qa-failed]']
      * Step 7: 'Release approval + deploy', RELEASE, Human ('Human QA/SA/Lead/PM'), evidence: ['L5'], primary: 'L5', state: 'Ready to Deploy', gate: 'human_verdict', tags: ['[deploying]']
      * Step 8: 'Smoke test & monitor', RELEASE, AI ('AI / Automation'), evidence: ['L6'], primary: 'L6', state: 'Ready to Deploy', gate: 'automated_trigger', tags: ['[deploy-regressed]', '[smoke-harness-error]']
      * Step 9: 'Retro takeaways, docs, skill enhancement', RETRO, AI ('AI Agent & Team'), evidence: ['L7'], primary: 'L7', state: 'Done', gate: 'automated_trigger', tags: ['[golden-path-complete]', '[retro-failed]']
    - Export lookup functions:
      * resolveRoutingStep(state: string, tags?: string[] | null): StepDefinition | undefined (maps '[awaiting-input]' tag to step 3, 'New' -> step 1, 'Ready to Dev' -> step 2, 'In Dev' -> step 3, 'Dev Done' -> step 4, 'Ready for QA' -> step 6, 'Ready to Deploy' -> step 7, 'Done' -> step 9, unhandled -> undefined)
      * getStepByNumber(step: StepNumber): StepDefinition | undefined
      * getStepsByColumn(column: ColumnId): readonly StepDefinition[]
      * getStepsByAdoState(state: string): readonly StepDefinition[]
    - Add ponytail comment: '// ponytail: static step descriptors; load from project taxonomy schema in v2.x'
  </behavior>
  <action>
    Create `src/pipeline/taxonomy.ts` implementing the complete types, frozen `GOLDEN_PATH_V2` array, and lookup functions exactly matching the specifications and pattern in 02-PATTERNS.md lines 20-128.
  </action>
  <verify>
    <automated>npx tsx -e "import { GOLDEN_PATH_V2, resolveRoutingStep } from './src/pipeline/taxonomy.js'; console.log('Steps loaded:', GOLDEN_PATH_V2.length, 'Step 1:', resolveRoutingStep('New')?.name);"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pipeline/taxonomy.ts` has zero imports and zero external runtime dependencies.
    - `GOLDEN_PATH_V2` contains exactly 9 step objects and is frozen with Object.isFrozen.
    - `resolveRoutingStep('New')` returns Step 1; `resolveRoutingStep('In Dev')` returns Step 3; `resolveRoutingStep('Dev Done')` returns Step 4.
  </acceptance_criteria>
  <done>Taxonomy module created with complete 9-step Golden Path v2 definitions and resolution functions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement taxonomy unit tests verifying structure, immutability, and resolution mappings</name>
  <files>tests/taxonomy.test.ts</files>
  <read_first>
    - src/pipeline/taxonomy.ts
    - .planning/phases/02-taxonomy-foundation/02-PATTERNS.md
    - tests/verdict-detector.test.ts
  </read_first>
  <behavior>
    - Test Golden Path v2 definition covers 9 steps (1..9), 5 columns (REFINEMENT, EXECUTION, ACCEPTANCE, RELEASE, RETRO), and all evidence levels L1-L7.
    - Test immutability: Object.isFrozen(GOLDEN_PATH_V2) is true, and every step object and evidenceLevels array is frozen.
    - Test actor roles and detail strings for each step (AI vs Human).
    - Test resolveRoutingStep mappings:
      * 'New' -> step 1
      * 'Ready to Dev' -> step 2
      * 'In Dev' -> step 3
      * Tag '[awaiting-input]' with any state -> step 3
      * 'Dev Done' -> step 4
      * 'Ready for QA' -> step 6
      * 'Ready to Deploy' -> step 7
      * 'Done' -> step 9
      * Unknown state -> undefined
    - Test helper functions getStepByNumber, getStepsByColumn, and getStepsByAdoState.
  </behavior>
  <action>
    Create `tests/taxonomy.test.ts` implementing unit tests verifying TAX-01 requirements and TAX-03 lookup mapping assertions following pattern in 02-PATTERNS.md lines 295-373.
  </action>
  <verify>
    <automated>npx vitest run tests/taxonomy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/taxonomy.test.ts` passes with all tests green.
    - Verifies immutability, 9 steps, 5 columns, L1-L7 evidence levels, and all resolution helpers.
  </acceptance_criteria>
  <done>Unit test suite passing, proving taxonomy data contract and resolution logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Module Consumer -> Taxonomy Data | Downstream modules or external caller attempting to modify or monkey-patch taxonomy constants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `src/pipeline/taxonomy.ts` | mitigate | Recursively freeze `GOLDEN_PATH_V2` array and all step definitions using `Object.freeze` to make taxonomy immutable at runtime |
</threat_model>

<verification>
Automated test suite execution:
`npx vitest run tests/taxonomy.test.ts`
</verification>

<success_criteria>
1. `src/pipeline/taxonomy.ts` exports frozen `GOLDEN_PATH_V2` with 9 steps, 5 columns, actors, evidence levels, and ADO states.
2. `src/pipeline/taxonomy.ts` has zero runtime dependencies and imports no external modules.
3. `tests/taxonomy.test.ts` executes in <2s and passes with 100% green assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-taxonomy-foundation/02-01-SUMMARY.md`
</output>
