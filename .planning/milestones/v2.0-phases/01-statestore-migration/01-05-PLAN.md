---
phase: 01-statestore-migration
plan: 05
type: execute
wave: 4
depends_on:
  - "01-02"
  - "01-03"
  - "01-04"
files_modified:
  - src/execute/worker.ts
  - src/execute/router.ts
  - src/execute/rework-worker.ts
  - src/test-runner/evidence.ts
  - src/deploy/worker.ts
  - src/deploy/telemetry.ts
  - src/deploy/evidence-index.ts
  - src/learn/harvester.ts
  - src/learn/worker.ts
  - package.json
autonomous: true
requirements:
  - STATE-01
  - STATE-04

must_haves:
  truths:
    - "All remaining pipeline workers (execution, test runner, deploy, learn) read and persist state exclusively via StateStore"
    - "Evidence index compiler resolves L1-L6 summaries directly from TicketState frontmatter without database joins"
    - "src/db/ directory is removed completely and zero imports from ../db/ or drizzle-orm remain anywhere in src/"
    - "better-sqlite3, drizzle-orm, drizzle-kit, and @types/better-sqlite3 are uninstalled and removed from package.json"
    - "The entire test suite (286 tests across 32 test files) passes 100% green on npm test using the mkdtemp test harness"
  artifacts:
    - path: "src/execute/worker.ts"
      provides: "Execution worker updating dedup status via StateStore"
      exports: ["processWorkItemExecution"]
    - path: "src/test-runner/evidence.ts"
      provides: "L3 evidence recorder appending directly to TicketState frontmatter"
      exports: ["recordL3Evidence", "formatL3EvidenceComment"]
    - path: "src/deploy/evidence-index.ts"
      provides: "L1-L6 unified evidence compiler reading directly from TicketState"
      exports: ["compileL1L6EvidenceIndex", "formatEvidenceIndexComment"]
    - path: "src/learn/harvester.ts"
      provides: "Lifecycle harvester reading rework and verification history from TicketState"
      exports: ["harvestTicketLifecycleData"]
    - path: "package.json"
      provides: "Package manifest without SQLite and Drizzle dependencies"
      contains: "\"dependencies\":"
  key_links:
    - from: "src/deploy/evidence-index.ts"
      to: "src/state/index.ts"
      via: "stateStore.getTicketState(workItemId)"
      pattern: "stateStore\\.getTicketState"
    - from: "src/learn/harvester.ts"
      to: "src/state/index.ts"
      via: "stateStore.getTicketState(workItemId)"
      pattern: "stateStore\\.getTicketState"
    - from: "src/test-runner/evidence.ts"
      to: "src/state/index.ts"
      via: "stateStore.updateTicketState"
      pattern: "stateStore\\.updateTicketState"
---

<objective>
Complete the StateStore migration across the remaining workers (execution, test runner, deploy, learn), eradicate `src/db/`, remove SQLite and Drizzle packages from `package.json`, and ensure the entire 286-test suite passes 100% green on the `mkdtemp` test harness.

Purpose: Finalize the total elimination of SQLite from the codebase with verified behavioral parity across all stages.
Output: Migrated workers, deleted `src/db/`, clean `package.json`, and all 32 test files passing green.
</objective>

<execution_context>
@$HOME/.config/antigravity/get-shit-done/workflows/execute-plan.md
@$HOME/.config/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-statestore-migration/01-RESEARCH.md
@.planning/phases/01-statestore-migration/01-PATTERNS.md
@package.json
@src/execute/worker.ts
@src/execute/router.ts
@src/execute/rework-worker.ts
@src/test-runner/evidence.ts
@src/deploy/worker.ts
@src/deploy/telemetry.ts
@src/deploy/evidence-index.ts
@src/learn/harvester.ts
@src/learn/worker.ts

<interfaces>
<!-- StateStore operations used by execution, deploy, and learn -->
import { stateStore } from '../state/index.js';
import type { TicketState } from '../state/types.js';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate Execution & Test Runner Subsystems to StateStore</name>
  <files>src/execute/worker.ts, src/execute/router.ts, src/execute/rework-worker.ts, src/test-runner/evidence.ts</files>
  <read_first>
    - src/execute/worker.ts
    - src/execute/router.ts
    - src/execute/rework-worker.ts
    - src/test-runner/evidence.ts
  </read_first>
  <behavior>
    - recordL3Evidence appends entry to draft.l3Evidence array inside stateStore.updateTicketState.
    - processWorkItemExecution and processWorkItemRework call stateStore.updateDedupStatus on completion/failure.
    - routeWorkItemEvent inspects ticket.l3Evidence via stateStore.getTicketState.
    - rework-worker.ts prioritizes local branch candidates (master, main) to resolve git base branch cleanly in test environments.
  </behavior>
  <action>
    1. Update `src/test-runner/evidence.ts`:
       - Remove `db`, `l3Evidence`, `InsertL3Evidence` imports from `../db/`.
       - Import `stateStore` from `../state/index.js`.
       - Update `recordL3Evidence`:
         ```typescript
         export async function recordL3Evidence(data: any): Promise<void> {
           await stateStore.updateTicketState(data.workItemId, (draft) => {
             draft.l3Evidence.push({
               revId: data.revId,
               testSuite: data.testSuite,
               totalTests: data.totalTests,
               passed: data.passed,
               failed: data.failed,
               durationMs: data.durationMs,
               gitDiffStat: typeof data.gitDiffStat === 'string' ? data.gitDiffStat : JSON.stringify(data.gitDiffStat),
               coverageSummary: data.coverageSummary || null,
               createdAt: new Date().toISOString(),
             });
           });
         }
         ```
    2. Update `src/execute/worker.ts`:
       - Remove `db`, `dedupEvents`, `eq`, `and` imports.
       - Import `stateStore` from `../state/index.js`.
       - Replace `db.update(dedupEvents)` with `stateStore.updateDedupStatus(workItemId, revId, 'completed')` (and `'failed'` in catch).
    3. Update `src/execute/rework-worker.ts`:
       - Remove `db`, `dedupEvents` imports.
       - Import `stateStore` from `../state/index.js`.
       - Replace `db.update(dedupEvents)` with `stateStore.updateDedupStatus`.
       - In candidate base branch resolution (lines 88-99): check local branches `['master', 'main', 'origin/main', 'origin/master']` so local checkouts in test runners resolve the clean local commit history.
    4. Update `src/execute/router.ts`:
       - Remove `db`, `dedupEvents`, `l3Evidence` imports.
       - Import `stateStore` from `../state/index.js`.
       - In L3 verification check: fetch ticket via `const ticket = await stateStore.getTicketState(workItemId);` and check `ticket?.l3Evidence`.
  </action>
  <verify>
    <automated>npx vitest run tests/l3-evidence.test.ts tests/rework-integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/execute/` and `src/test-runner/` contain zero imports from `../db/`.
    - `tests/l3-evidence.test.ts` passes using StateStore.
    - `tests/rework-integration.test.ts` passes with correct base branch resolution.
  </acceptance_criteria>
  <done>Execution, router, and test runner evidence subsystems migrated cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate Deploy & Learn Subsystems to StateStore</name>
  <files>src/deploy/worker.ts, src/deploy/telemetry.ts, src/deploy/evidence-index.ts, src/learn/harvester.ts, src/learn/worker.ts</files>
  <read_first>
    - src/deploy/worker.ts
    - src/deploy/telemetry.ts
    - src/deploy/evidence-index.ts
    - src/learn/harvester.ts
    - src/learn/worker.ts
  </read_first>
  <behavior>
    - deploymentRecords and telemetryEvaluations persist to ticket frontmatter arrays.
    - compileL1L6EvidenceIndex reads L1-L6 directly from getTicketState(workItemId) and persists index summary to draft.evidenceIndex.
    - harvestTicketLifecycleData loads rework, l3, qa, and telemetry directly from getTicketState(workItemId).
    - skillsPrs records persist to draft.skillsPrs.
  </behavior>
  <action>
    1. Update `src/deploy/worker.ts`:
       - Remove `db`, `deploymentRecords` imports. Import `stateStore` from `../state/index.js`.
       - In `processDeploymentWorkflow`: record deployment via `stateStore.updateTicketState(workItemId, draft => { draft.deploymentRecords.push(...) })`.
    2. Update `src/deploy/telemetry.ts`:
       - Remove `db`, `telemetryEvaluations` imports. Import `stateStore` from `../state/index.js`.
       - In `evaluateTelemetry`: record evaluation via `stateStore.updateTicketState(workItemId, draft => { draft.telemetryEvaluations.push(...) })`.
    3. Update `src/deploy/evidence-index.ts`:
       - Remove `db` and all schema table imports. Import `stateStore` from `../state/index.js`.
       - In `compileL1L6EvidenceIndex(workItemId)`:
         - Load ticket: `const ticket = await stateStore.getTicketState(workItemId);`
         - Extract latest entries from `ticket?.auditLogs`, `ticket?.l3Evidence`, `ticket?.qaEvidence`, `ticket?.deploymentRecords`, `ticket?.telemetryEvaluations`.
         - Build summary object.
         - Persist to ticket:
           ```typescript
           await stateStore.updateTicketState(workItemId, (draft) => {
             draft.evidenceIndex = {
               l1Summary: JSON.stringify(summary.l1),
               l2Summary: JSON.stringify(summary.l2),
               l3Summary: JSON.stringify(summary.l3),
               l4Summary: JSON.stringify(summary.l4),
               l5Summary: JSON.stringify(summary.l5),
               l6Summary: JSON.stringify(summary.l6),
               completedAt: new Date().toISOString(),
             };
           });
           ```
    4. Update `src/learn/harvester.ts`:
       - Remove `db` and table imports. Import `stateStore` from `../state/index.js`.
       - Read ticket via `await stateStore.getTicketState(workItemId)` and populate `TicketLifecycleData`.
    5. Update `src/learn/worker.ts`:
       - Remove `db`, `skillsPrs` imports. Import `stateStore` from `../state/index.js`.
       - Persist created PR via `stateStore.updateTicketState(workItemId, draft => { draft.skillsPrs.push(...) })`.
  </action>
  <verify>
    <automated>npx vitest run tests/deploy-packet.test.ts tests/deploy-telemetry.test.ts tests/deploy-orchestrator.test.ts tests/learn-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/deploy/` and `src/learn/` contain zero imports from `../db/`.
    - `compileL1L6EvidenceIndex` operates without database queries.
    - Deploy and learn tests pass on file-backed store.
  </acceptance_criteria>
  <done>Deploy, evidence-index, and learning subsystems running on StateStore.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove src/db/, Uninstall SQLite/Drizzle Packages, and Verify Full 286-Test Suite Green</name>
  <files>package.json</files>
  <read_first>
    - package.json
    - src/config/env.ts
  </read_first>
  <behavior>
    - better-sqlite3, drizzle-orm, drizzle-kit, and @types/better-sqlite3 are removed from package.json.
    - src/db/ directory is deleted.
    - All 32 test files execute and pass 100% on npm test with mkdtemp test harness.
  </behavior>
  <action>
    1. Remove all files in `src/db/` (`src/db/index.ts`, `src/db/schema.ts`) and delete the directory.
    2. In `src/config/env.ts`: remove `DATABASE_PATH` completely from `EnvSchema`.
    3. Remove dependencies from `package.json`:
       - Remove `better-sqlite3`, `drizzle-orm` from `dependencies`.
       - Remove `@types/better-sqlite3`, `drizzle-kit` from `devDependencies`.
       - Run `npm install` to update `package-lock.json`.
    4. Verify every test file in `tests/`:
       - Replace any lingering `import { db, sqlite } from '../src/db/index.js'` with `createTestStateStore()`.
       - Run `npm test` across all 32 test files.
       - Ensure all 286 tests pass green without regressions.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Zero references to SQLite or Drizzle exist in `package.json` or `src/`.
    - `src/db/` directory does not exist.
    - `npm test` reports all 32 test files passing (286+ tests, 0 failures).
  </acceptance_criteria>
  <done>Phase 1 complete: SQLite completely eliminated and full suite 100% green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evidence Index ↔ StateStore | Summary generation compiles verifiable evidence from multi-stage records |
| Subprocess Execution ↔ Test Runner | vitest runner and execa commands execute in isolated mkdtemp folders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Repudiation | compileL1L6EvidenceIndex | mitigate | Persist evidence index directly in ticket frontmatter with ISO timestamp and sanitized summaries |
| T-01-12 | Information Disclosure | package.json cleanup | mitigate | Remove C++ binary dependencies (better-sqlite3) to reduce supply chain and native build attack surface |
</threat_model>

<verification>
Automated full test suite run:
`npm test`
</verification>

<success_criteria>
1. All workers across the pipeline persist through the backend-agnostic StateStore.
2. `better-sqlite3`, `drizzle-orm`, `drizzle-kit`, and `@types/better-sqlite3` are uninstalled.
3. `src/db/` is completely removed.
4. All 32 test files pass 100% green on `npm test`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-statestore-migration/01-05-SUMMARY.md`
</output>
